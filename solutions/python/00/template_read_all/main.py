import sys


def main() -> None:
    data = sys.stdin.read().split()
    # data[0], data[1], ... が文字列
    # 整数にする例:
    # it = iter(data)
    # n = int(next(it))
    # xs = [int(next(it)) for _ in range(n)]
    pass


if __name__ == "__main__":
    main()
