# Part 4 — 水色（Cyan）

**目標**: ABC の **D〜E** と **ARC A〜B** を解く。**DSU・最短経路・2D 累積和・応用 DP・Fenwick/セグ木・mod 逆元** を実戦投入する。  
解説は **ゆっくり霊夢** と **ゆっくり魔理沙** の対談形式。

**リポジトリ上のコード（Part 4）**

| 種別 | 一覧 |
|------|------|
| Python | [solutions/python/04/](../solutions/python/04/) |
| Go | [solutions/go/04/](../solutions/go/04/) |

← [03.md](03.md)（緑） | [目次](00-toc.md) | 次 → [05.md](05.md)（青色）

---

## 4.1 Union-Find（DSU）

**霊夢**: グループの合体・同じグループか判定って、毎回 DFS するの？

**魔理沙**: **Union-Find（DSU）** を使え。ほぼ **逆アッカーマン** で、`unite` と `find` が高速だ。

| 操作 | 意味 |
|------|------|
| `find(x)` | x の代表元（根） |
| `unite(a, b)` | a と b のグループを合体 |
| `same(a, b)` | 同じグループか |

#### DSU（Python）

→ [solutions/python/04/dsu/](../solutions/python/04/dsu/) / [solutions/go/04/dsu/](../solutions/go/04/dsu/)

```python
class DSU:
    def __init__(self, n: int) -> None:
        self.parent = list(range(n))
        self.size = [1] * n

    def find(self, x: int) -> int:
        while self.parent[x] != x:
            self.parent[x] = self.parent[self.parent[x]]
            x = self.parent[x]
        return x

    def unite(self, a: int, b: int) -> bool:
        a, b = self.find(a), self.find(b)
        if a == b:
            return False
        if self.size[a] < self.size[b]:
            a, b = b, a
        self.parent[b] = a
        self.size[a] += self.size[b]
        return True
```

```go
type DSU struct {
    parent []int
    size   []int
}

func (d *DSU) Find(x int) int {
    for d.parent[x] != x {
        d.parent[x] = d.parent[d.parent[x]]
        x = d.parent[x]
    }
    return x
}

func (d *DSU) Unite(a, b int) bool {
    a, b = d.Find(a), d.Find(b)
    if a == b {
        return false
    }
    if d.size[a] < d.size[b] {
        a, b = b, a
    }
    d.parent[b] = a
    d.size[a] += d.size[b]
    return true
}
```

#### クラスカル（最小全域木の準備）

→ [solutions/python/04/kruskal/](../solutions/python/04/kruskal/) / [solutions/go/04/kruskal/](../solutions/go/04/kruskal/)

辺を重み昇順に見て、端点が別連結成分なら採用（`unite` 成功）。

```python
edges.sort(key=lambda e: e[2])
dsu = DSU(n)
total = 0
for u, v, w in edges:
    if dsu.unite(u, v):
        total += w
```

### 🖐️ abc177_d 相当（クエリで合体・同グループの組数）

→ [solutions/python/04/abc177_d/](../solutions/python/04/abc177_d/) / [solutions/go/04/abc177_d/](../solutions/go/04/abc177_d/)

「同じグループにいるペア数」を DSU の `size` で数える典型（ABC E / クエリ）。

---

## 4.2 最短経路

**霊夢**: [03.md](03.md) でダイクストラはやった。他に何があるの？

**魔理沙**: 水色では **3 種類** を使い分ける。

| 手法 | 条件 | 計算量 |
|------|------|--------|
| ダイクストラ | 非負辺 | O((V+E) log V) |
| 0-1 BFS | 辺コスト 0 または 1 | O(V+E) |
| ワーシャル | V ≤ 400 程度 | O(V^3) |

#### 0-1 BFS（deque）

→ [solutions/python/04/bfs01/](../solutions/python/04/bfs01/) / [solutions/go/04/bfs01/](../solutions/go/04/bfs01/)

```python
from collections import deque

def bfs01(g, start, n):
    INF = 10**18
    dist = [INF] * n
    dist[start] = 0
    dq = deque([start])
    while dq:
        v = dq.popleft()
        for to, cost in g[v]:
            nd = dist[v] + cost
            if nd < dist[to]:
                dist[to] = nd
                if cost == 0:
                    dq.appendleft(to)
                else:
                    dq.append(to)
    return dist
```

```go
// cost 0 なら先頭、1 なら末尾に push
```

#### ワーシャル（全点対）

→ [solutions/python/04/floyd_warshall/](../solutions/python/04/floyd_warshall/) / [solutions/go/04/floyd_warshall/](../solutions/go/04/floyd_warshall/)

```python
def floyd(dist):
    n = len(dist)
    for k in range(n):
        for i in range(n):
            for j in range(n):
                dist[i][j] = min(dist[i][j], dist[i][k] + dist[k][j])
    return dist
```

### 🖐️ abc184_e 相当（グリッド + テレポート = 0-1）

[abc184_e](https://atcoder.jp/contests/abc184/tasks/abc184_e) は **隣接移動コスト 0・同じ文字へテレポートコスト 1** の 0-1 BFS 定番。実装は [bfs01/](../solutions/python/04/bfs01/) の拡張で挑戦する。

---

## 4.3 累積和・二次元累積和

**霊夢**: 矩形の和、毎回四重ループは無理でしょ？

**魔理沙**: **2 次元累積和** で、矩形 `[h1,w1]`〜`[h2,w2]` の和を O(1) だ。

#### 構築（1-indexed）

→ [solutions/python/04/prefix_sum_2d/](../solutions/python/04/prefix_sum_2d/) / [solutions/go/04/prefix_sum_2d/](../solutions/go/04/prefix_sum_2d/)

```python
def build_2d(a):
    h, w = len(a), len(a[0])
    s = [[0] * (w + 1) for _ in range(h + 1)]
    for i in range(1, h + 1):
        for j in range(1, w + 1):
            s[i][j] = s[i - 1][j] + s[i][j - 1] - s[i - 1][j - 1] + a[i - 1][j - 1]
    return s

def rect_sum(s, h1, w1, h2, w2):
    return s[h2][w2] - s[h1 - 1][w2] - s[h2][w1 - 1] + s[h1 - 1][w1 - 1]
```

```go
// s[i][j] = 上+左-左上+元の値
func rectSum(s [][]int, h1, w1, h2, w2 int) int {
    return s[h2][w2] - s[h1-1][w2] - s[h2][w1-1] + s[h1-1][w1-1]
}
```

**霊夢**: 1-indexed なのが、毎回つまずく……

**魔理沙**: `s[0][*]=s[*][0]=0` のガード行を足すと式がきれいになる。紙に書いてからコードにする癖をつけろ。

---

## 4.4 DP の応用

**霊夢**: ナップサックの次は何？

**魔理沙**: 水色では **区間 DP・木 DP・bitmask DP** の 3 つが頻出だ。

### 区間 DP（例: 石の合体）

`dp[l][r]` = 区間 `[l,r]` の最適値。長さで増やす。

→ [solutions/python/04/dp_interval/](../solutions/python/04/dp_interval/) / [solutions/go/04/dp_interval/](../solutions/go/04/dp_interval/)

```python
def merge_stones(a):
    n = len(a)
    dp = [[0] * n for _ in range(n)]
    for length in range(2, n + 1):
        for l in range(n - length + 1):
            r = l + length - 1
            dp[l][r] = min(
                dp[l][k] + dp[k + 1][r] for k in range(l, r)
            ) + sum(a[l : r + 1])
    return dp[0][n - 1]
```

### 木 DP（部分木の値）

→ [solutions/python/04/dp_tree/](../solutions/python/04/dp_tree/) / [solutions/go/04/dp_tree/](../solutions/go/04/dp_tree/)

```python
import sys

sys.setrecursionlimit(10**7)

def dfs(v, parent, g, dp):
    dp[v] = 1  # 例: 部分木のサイズ
    for to in g[v]:
        if to == parent:
            continue
        dfs(to, v, g, dp)
        dp[v] += dp[to]
```

### bitmask DP（小さな N）

`dp[mask][i]` = 訪問済み `mask`、現在 `i`。TSP 入門。

→ [solutions/python/04/dp_bitmask/](../solutions/python/04/dp_bitmask/) / [solutions/go/04/dp_bitmask/](../solutions/go/04/dp_bitmask/)

```python
def tsp(dist, n):
    INF = 10**18
    dp = [[INF] * n for _ in range(1 << n)]
    dp[1][0] = 0
    for mask in range(1 << n):
        for i in range(n):
            if dp[mask][i] == INF:
                continue
            for j in range(n):
                if mask >> j & 1:
                    continue
                nmask = mask | (1 << j)
                dp[nmask][j] = min(dp[nmask][j], dp[mask][i] + dist[i][j])
    full = (1 << n) - 1
    return min(dp[full][i] for i in range(n))
```

---

## 4.5 セグメントツリー・Fenwick（入門）

**霊夢**: 区間和を更新し続ける問題って、累積和じゃ足りないの？

**魔理沙**: **点更新 + 区間和** が繰り返されるときは **Fenwick（BIT）** か **セグメントツリー** を使う。

| 構造 | できること（入門） | 実装のしやすさ |
|------|-------------------|----------------|
| Fenwick | 点更新・区間和 | ◎ 短い |
| セグメントツリー | 点更新・区間和（拡張しやすい） | △ やや長い |

#### Fenwick（Python）

→ [solutions/python/04/fenwick/](../solutions/python/04/fenwick/) / [solutions/go/04/fenwick/](../solutions/go/04/fenwick/)

```python
class Fenwick:
    def __init__(self, n: int) -> None:
        self.n = n
        self.bit = [0] * (n + 1)

    def add(self, i: int, x: int) -> None:
        while i <= self.n:
            self.bit[i] += x
            i += i & -i

    def sum(self, i: int) -> int:
        s = 0
        while i > 0:
            s += self.bit[i]
            i -= i & -i
        return s

    def range_sum(self, l: int, r: int) -> int:
        return self.sum(r) - self.sum(l - 1)
```

```go
type Fenwick struct {
    n   int
    bit []int
}

func (f *Fenwick) Add(i, x int) {
    for i <= f.n {
        f.bit[i] += x
        i += i & -i
    }
}
```

#### セグメントツリー（区間和・一点更新）

→ [solutions/python/04/segtree/](../solutions/python/04/segtree/) / [solutions/go/04/segtree/](../solutions/go/04/segtree/)

```python
class SegTree:
    def __init__(self, data):
        n = 1
        while n < len(data):
            n <<= 1
        self.n = n
        self.seg = [0] * (2 * n)
        for i, v in enumerate(data):
            self.seg[n + i] = v
        for i in range(n - 1, 0, -1):
            self.seg[i] = self.seg[2 * i] + self.seg[2 * i + 1]

    def update(self, i, x):
        i += self.n
        self.seg[i] = x
        while i > 1:
            i >>= 1
            self.seg[i] = self.seg[2 * i] + self.seg[2 * i + 1]

    def query(self, l, r):
        l += self.n
        r += self.n
        res = 0
        while l < r:
            if l & 1:
                res += self.seg[l]
                l += 1
            if r & 1:
                r -= 1
                res += self.seg[r]
            l >>= 1
            r >>= 1
        return res
```

**霊夢**: lazy propagation は？

**魔理沙**: 区間更新が必要になったら [05.md](05.md) で扱う。水色では **一点更新・区間和** までで十分だ。

---

## 4.6 スライディングウィンドウ・尺取り

**霊夢**: 部分列で条件を満たす longest / shortest、どう探すの？

**魔理沙**: **尺取り法（two pointers）** だ。右端を伸ばし、条件を破したら左端を縮める。

→ [solutions/python/04/two_pointers/](../solutions/python/04/two_pointers/) / [solutions/go/04/two_pointers/](../solutions/go/04/two_pointers/)

「**合計が K 以下** の部分列の個数」を数える例。

```python
def count_subarray_le_k(xs, k):
    n = len(xs)
    ans = 0
    left = 0
    s = 0
    for right in range(n):
        s += xs[right]
        while s > k and left <= right:
            s -= xs[left]
            left += 1
        ans += right - left + 1
    return ans
```

```go
ans, left, sum := 0, 0, 0
for right := 0; right < n; right++ {
    sum += xs[right]
    for sum > k && left <= right {
        sum -= xs[left]
        left++
    }
    ans += right - left + 1
}
```

**魔理沙**: 「最短の長さ」を求める問題では、条件を満たしたら左を進めるパターンも多い。文面で **最大化か最小化か** を確認しろ。

---

## 4.7 数学：逆元・フェルマ

**霊夢**: nCr mod、割り算ってどうするの？

**魔理沙**: mod が素数 `p` のとき、**逆元** `a^{-1}` は `a^{p-2} mod p`（フェルマの小定理）。[03.md](03.md) のパスカルより高速に大量の nCr が欲しいときは **階乗 + 逆元** を使う。

→ [solutions/python/04/modinv/](../solutions/python/04/modinv/) / [solutions/go/04/modinv/](../solutions/go/04/modinv/)

```python
MOD = 998244353

def mod_pow(a, e, mod=MOD):
    res = 1
    while e:
        if e & 1:
            res = res * a % mod
        a = a * a % mod
        e >>= 1
    return res

def prepare_fac(n, mod=MOD):
    fac = [1] * (n + 1)
    inv = [1] * (n + 1)
    for i in range(1, n + 1):
        fac[i] = fac[i - 1] * i % mod
    inv[n] = mod_pow(fac[n], mod - 2, mod)
    for i in range(n, 0, -1):
        inv[i - 1] = inv[i] * i % mod
    return fac, inv

def ncr(n, r, fac, inv, mod=MOD):
    if r < 0 or r > n:
        return 0
    return fac[n] * inv[r] % mod * inv[n - r] % mod
```

```go
func modPow(a, e, mod int) int { /* 繰り返し二乗 */ }
// fac, inv を前計算して nCr
```

**霊夢**: mod が素数じゃないときは？

**魔理沙**: 拡張ユークリッドで逆元を求める。水色では **998244353** 前提で十分なことが多い。

---

## 4.8 二分グラフ・マッチング入門

**霊夢**: 2 色に分けられるか、ってグラフの問題？

**魔理沙**: **二分グラフ判定** は BFS/DFS で「色 0/1 を交互に塗る」。矛盾したら No。

→ [solutions/python/04/bipartite/](../solutions/python/04/bipartite/) / [solutions/go/04/bipartite/](../solutions/go/04/bipartite/)

```python
from collections import deque

def is_bipartite(g, n):
    color = [-1] * n
    for s in range(n):
        if color[s] != -1:
            continue
        color[s] = 0
        q = deque([s])
        while q:
            v = q.popleft()
            for to in g[v]:
                if color[to] == -1:
                    color[to] = color[v] ^ 1
                    q.append(to)
                elif color[to] == color[v]:
                    return False
    return True
```

**マッチング入門**: 水色 ARC B では **貪欲** や **DFS で増路** の簡単版が出る。本格は [05.md](05.md) のフローへ。

### 🖐️ abc199_d 相当（2 色塗り）

[abc199_d](https://atcoder.jp/contests/abc199/tasks/abc199_d) は **隣接頂点が異なる文字** の割当。二分グラフ + 小規模なら全探索のハイブリッドが典型。

---

## 4.9 水色まとめハンズオン

**霊夢**: ARC B って、急に難しく感じる……

**魔理沙**: 緑で培った **グラフ・DP・データ構造** を組み合わせるだけだ。次の表を進めろ。最後に **初 AGC A** に触れる。

| 順 | 問題 ID | 想定スキル | 目安 |
|----|---------|------------|------|
| 1 | [abc177_d](https://atcoder.jp/contests/abc177/tasks/abc177_d) | DSU（[解答](../solutions/python/04/abc177_d/)） | E |
| 2 | [abc184_e](https://atcoder.jp/contests/abc184/tasks/abc184_e) | 0-1 BFS | E |
| 3 | [abc165_e](https://atcoder.jp/contests/abc165/tasks/abc165_e) | 木・累積 |
| 4 | [abc199_d](https://atcoder.jp/contests/abc199/tasks/abc199_d) | 二分グラフ | D |
| 5 | [abc180_e](https://atcoder.jp/contests/abc180/tasks/abc180_e) | bitmask DP | E |
| 6 | [abc187_e](https://atcoder.jp/contests/abc187/tasks/abc187_e) | 木 DP | E |
| 7 | [abc214_e](https://atcoder.jp/contests/abc214/tasks/abc214_e) | 累積和・ソート | E |
| 8 | [arc115_a](https://atcoder.jp/contests/arc115/tasks/arc115_a) | 全探索・文字列 | ARC A |
| 9 | [arc118_a](https://atcoder.jp/contests/arc118/tasks/arc118_a) | 貪欲・数論 | ARC A |
| 10 | [arc120_a](https://atcoder.jp/contests/arc120/tasks/arc120_a) | 幾何・シミュレーション | ARC A |
| 11 | [agc015_a](https://atcoder.jp/contests/agc015/tasks/agc015_a) | 貪欲（**初 AGC A**） | AGC A |

**初 AGC A の心構え**

- 問題文が長くても、求めている操作は **シンプル** なことが多い。  
- まずは **手で小さい例** を試し、貪欲・xor・ソートのどれか当てる。  
- 無理なら部分点や次の Part を見る（水色では **体験** で十分）。  

索引: [12.md](12.md)

---

## Part 4 到達チェック

- [ ] DSU で `unite` / `same` が書ける  
- [ ] 0-1 BFS またはワーシャルを説明できる  
- [ ] 2D 累積和で矩形和が O(1) で出せる  
- [ ] 区間 DP・bitmask DP の骨格を書ける  
- [ ] Fenwick で点更新・区間和ができる  
- [ ] 尺取りで部分列を数えられる  
- [ ] `nCr mod p` を階乗 + 逆元で出せる  
- [ ] 二分グラフ判定が書ける  
- [ ] まとめで ABC E を **2 問以上 AC**、ARC B を **1 問試す**、AGC A を **1 回読んで提出**  

**霊夢**: データ構造、だいぶ武器が増えた気がする。

**魔理沙**: [05.md](05.md) は **最小全域木・フロー・lazy セグ木** だ。水色の DSU とダイクストラがそのまま繋がる。進むぜ。

---

## まとめ

| 節 | 内容 |
|----|------|
| 4.1 | Union-Find、クラスカル |
| 4.2 | 0-1 BFS、ワーシャル |
| 4.3 | 2D 累積和 |
| 4.4 | 区間 DP、木 DP、bitmask DP |
| 4.5 | Fenwick、セグメントツリー入門 |
| 4.6 | 尺取り法 |
| 4.7 | 逆元、nCr 高速化 |
| 4.8 | 二分グラフ |
| 4.9 | ABC E + ARC + 初 AGC A |

← [03.md](03.md) | [目次](00-toc.md) | 次 → [05.md](05.md)（青色）
