# Part 2 — 茶色（Brown）

**目標**: ABC の **B〜C** を自力で解く。**ソート・貪欲・累積和・グリッド・座標圧縮** の型を身につける。  
解説は **ゆっくり霊夢** と **ゆっくり魔理沙** の対談形式。

**リポジトリ上のコード（Part 2）**

| 種別 | 一覧 |
|------|------|
| Python | [solutions/python/02/](../solutions/python/02/) |
| Go | [solutions/go/02/](../solutions/go/02/) |

← [01.md](01.md)（灰色） | [目次](00-toc.md) | 次 → [03.md](03.md)（緑）

---

## 2.1 ソートとカスタム比較

**霊夢**: [01.md](01.md) まででループは慣れた。茶色になると何が増えるの？

**魔理沙**: まず **ソート** だ。並べ替えたあとに貪欲、二分、累積和と組み合わせる。茶色の B〜C では「ソートしてから何かする」が定番だぜ。

**霊夢**: Python でソートするには？

**魔理沙**: リストの `sort` か、組み込みの `sorted` だ。

#### 昇順・降順（Python）

```python
xs = [3, 1, 4, 1]
xs.sort()                 # 昇順（元のリストを書き換え）
print(xs)                 # [1, 1, 3, 4]

ys = sorted(xs, reverse=True)  # 降順（新しいリスト）
```

#### 昇順・降順（Go）

→ [solutions/go/02/sort_pairs/](../solutions/go/02/sort_pairs/)

```go
import "sort"

xs := []int{3, 1, 4, 1}
sort.Ints(xs)  // 昇順

sort.Slice(xs, func(i, j int) bool {
    return xs[i] > xs[j]  // 降順
})
```

#### ペアを第 2 要素でソート（Python）

→ [solutions/python/02/sort_pairs/](../solutions/python/02/sort_pairs/)

```python
pairs = [(1, 3), (2, 1), (4, 2)]
pairs.sort(key=lambda x: x[1])   # 第 2 要素で昇順
print(pairs)  # [(2, 1), (4, 2), (1, 3)]
```

```python
# インデックス付きでソート（元の順番が欲しいとき）
items = [("b", 2), ("a", 1)]
items.sort(key=lambda t: t[1])
```

#### ペアのソート（Go）

```go
type pair struct{ a, b int }

ps := []pair{{1, 3}, {2, 1}, {4, 2}}
sort.Slice(ps, func(i, j int) bool {
    return ps[i].b < ps[j].b
})
```

**霊夢**: `key` って何？

**魔理沙**: 「どの値を基準に並べるか」を渡す関数だ。競プロでは **終了時刻**、**コスト**、**頻度** などで並べる。

---

## 2.2 貪欲法（入門）

**霊夢**: 貪欲って、目の前で一番良さそうなのを選び続けるやつ？

**魔理沙**: その通り。証明は問題ごとだが、茶色までは **「ソートしてから順に取る」** パターンを覚えれば十分なことが多い。

### 区間スケジューリング（終了時刻が早い順）

**問題の型**: 区間 `[l, r)` がたくさん。重ならないように最大何個選べるか。

**魔理沙**: **終了時刻 `r` の昇順** にソートし、取れるものから貪欲に取る。

→ [solutions/python/02/greedy_intervals/](../solutions/python/02/greedy_intervals/) / [solutions/go/02/greedy_intervals/](../solutions/go/02/greedy_intervals/)

**入力例**:

```text
3
1 3
2 4
3 5
```

**Python**:

```python
import sys

def main() -> None:
    input = sys.stdin.readline
    n = int(input())
    intervals = []
    for _ in range(n):
        l, r = map(int, input().split())
        intervals.append((l, r))
    intervals.sort(key=lambda x: x[1])  # 終了時刻でソート

    last = -10**18
    cnt = 0
    for l, r in intervals:
        if l >= last:  # 前の区間と重ならない（半開区間なら l >= last でよい例）
            cnt += 1
            last = r
    print(cnt)

if __name__ == "__main__":
    main()
```

**Go**:

```go
sort.Slice(intervals, func(i, j int) bool {
    return intervals[i].end < intervals[j].end
})
last := -1 << 60
cnt := 0
for _, iv := range intervals {
    if iv.start >= last {
        cnt++
        last = iv.end
    }
}
fmt.Println(cnt)
```

### 🖐️ コインの例（大きい額面から）

**魔理沙**: 「500 円、100 円、50 円…でちょうど払う」は、**大きいコインから使う** 貪欲が典型（この問題設定では最適になる例が多い）。

```python
coins = [500, 100, 50, 10, 5, 1]
amount = 1234
ans = 0
for c in coins:
    ans += amount // c
    amount %= c
print(ans)
```

```go
coins := []int{500, 100, 50, 10, 5, 1}
amount := 1234
ans := 0
for _, c := range coins {
    ans += amount / c
    amount %= c
}
fmt.Println(ans)
```

### 🖐️ abc142_b 相当（ソート + 貪欲）

→ [solutions/python/02/abc142_b/](../solutions/python/02/abc142_b/) / [solutions/go/02/abc142_b/](../solutions/go/02/abc142_b/)

価格を昇順に並べ、**K 個買うたびにその中で最安が半額** になるルールで総額を求める（ABC B 相当）。

```python
import sys

def main() -> None:
    input = sys.stdin.readline
    n = int(input())
    a = sorted(map(int, input().split()))
    k = 2  # 問題文の K（入力から読む版は solutions を参照）
    total = 0
    for i, price in enumerate(a):
        remaining = n - i
        if remaining % k == 0:
            total += price // 2
        else:
            total += price
    print(total)

if __name__ == "__main__":
    main()
```

**霊夢**: K は入力で来るんでしょ？

**魔理沙**: 本番は入力から読む。リポジトリの [abc142_b](../solutions/python/02/abc142_b/) は `N K` と価格を読む完成版だ。

---

## 2.3 二次元グリッド

**霊夢**: 迷路とか、マス目の問題も多いよね？

**魔理沙**: グリッドは **2 次元配列** で持つ。`H` 行 `W` 列、`grid[i][j]` は `i` 行目 `j` 列目（0-indexed）が競プロの約束だ。

#### グリッドの読み方（Python）

```python
import sys

def main() -> None:
    input = sys.stdin.readline
    h, w = map(int, input().split())
    grid = [list(input().strip()) for _ in range(h)]
    print(grid[0][0], grid[h - 1][w - 1])

if __name__ == "__main__":
    main()
```

#### 4 方向への移動

→ [solutions/python/02/grid_4dir/](../solutions/python/02/grid_4dir/) / [solutions/go/02/grid_4dir/](../solutions/go/02/grid_4dir/)

```python
H, W = 3, 4
grid = [
    list("..#."),
    list(".#.."),
    list("...."),
]
# 上, 下, 左, 右
di = [-1, 1, 0, 0]
dj = [0, 0, -1, 1]

si, sj = 1, 0
for k in range(4):
    ni, nj = si + di[k], sj + dj[k]
    if 0 <= ni < H and 0 <= nj < W and grid[ni][nj] != "#":
        print("ok", ni, nj)
```

```go
di := []int{-1, 1, 0, 0}
dj := []int{0, 0, -1, 1}
ni, nj := si+di[k], sj+dj[k]
if 0 <= ni && ni < H && 0 <= nj && nj < W && grid[ni][nj] != '#' {
    // 移動可能
}
```

**霊夢**: `#` は壁？

**魔理沙**: 問題文の記号に従え。`.` は通れる、`#` は壁、という問題が多い。

#### グリッド上の BFS（おまけ）

茶色の C では **幅優先探索** が出始める。緑 Part で本格化するが、ここではイメージだけ。

```python
from collections import deque

def bfs(grid, si, sj):
    h, w = len(grid), len(grid[0])
    dist = [[-1] * w for _ in range(h)]
    dist[si][sj] = 0
    q = deque([(si, sj)])
    while q:
        i, j = q.popleft()
        for di, dj in [(-1, 0), (1, 0), (0, -1), (0, 1)]:
            ni, nj = i + di, j + dj
            if 0 <= ni < h and 0 <= nj < w and grid[ni][nj] != "#" and dist[ni][nj] == -1:
                dist[ni][nj] = dist[i][j] + 1
                q.append((ni, nj))
    return dist
```

---

## 2.4 累積和・差分（1 次元）

**霊夢**: 区間の和、毎回ループしたら遅くない？

**魔理沙**: **累積和** を作れば、区間 `[l, r]` の和を O(1) で求められる。茶色の C で超頻出だ。

#### 累積和（Python）

→ [solutions/python/02/prefix_sum/](../solutions/python/02/prefix_sum/) / [solutions/go/02/prefix_sum/](../solutions/go/02/prefix_sum/)

```python
import sys

def main() -> None:
    input = sys.stdin.readline
    n, q = map(int, input().split())
    a = [0] + list(map(int, input().split()))  # 1-indexed にする
    # cum[i] = a[1] + ... + a[i]
    cum = [0] * (n + 1)
    for i in range(1, n + 1):
        cum[i] = cum[i - 1] + a[i]

    for _ in range(q):
        l, r = map(int, input().split())
        print(cum[r] - cum[l - 1])  # 区間 [l, r] の和

if __name__ == "__main__":
    main()
```

**入力例**:

```text
5 3
1 2 3 4 5
1 3
2 4
5 5
```

**出力**:

```text
6
9
5
```

#### 累積和（Go）

```go
cum := make([]int, n+1)
for i := 1; i <= n; i++ {
    cum[i] = cum[i-1] + a[i]
}
// [l, r] の和
ans := cum[r] - cum[l-1]
```

#### 差分配列（区間に一括 +1）

**魔理沙**: 「区間 `[l,r]` に +v」を何度もやるとき、差分配列 `diff` で O(1) に記録し、最後に累積和で戻す。

→ [solutions/python/02/diff_array/](../solutions/python/02/diff_array/) / [solutions/go/02/diff_array/](../solutions/go/02/diff_array/)

```python
n = 5
diff = [0] * (n + 2)

def add_range(l: int, r: int, v: int) -> None:
    diff[l] += v
    diff[r + 1] -= v

add_range(1, 3, 10)  # 1..3 に +10
add_range(2, 5, 5)   # 2..5 に +5

a = [0] * (n + 1)
for i in range(1, n + 1):
    a[i] = a[i - 1] + diff[i]
print(a[1:])  # 最終配列
```

```go
diff := make([]int, n+2)
addRange := func(l, r, v int) {
    diff[l] += v
    diff[r+1] -= v
}
```

**霊夢**: 差分って、茶色で毎回使うの？

**魔理沙**: C 問題の「区間更新」で出る。累積和とセットで覚えろ。

---

## 2.5 集合・辞書・カウンタ

**霊夢**: 同じ数が何回出たか、ってどう数える？

**魔理沙**: **辞書 / カウンタ** を使う。Python は `collections.Counter` が便利だ。

#### 頻度カウント（Python）

→ [solutions/python/02/counter_freq/](../solutions/python/02/counter_freq/) / [solutions/go/02/counter_freq/](../solutions/go/02/counter_freq/)

```python
from collections import Counter

xs = [1, 2, 2, 3, 3, 3]
cnt = Counter(xs)
print(cnt[2])        # 2
print(cnt.most_common(2))  # [(3, 3), (2, 2)]
```

```python
# 手動で辞書
freq = {}
for x in xs:
    freq[x] = freq.get(x, 0) + 1
```

#### 頻度カウント（Go）

```go
freq := map[int]int{}
for _, x := range xs {
    freq[x]++
}
```

#### 集合で重複除去

```python
s = set([1, 2, 2, 3])
print(len(s))  # 3
```

```go
seen := map[int]struct{}{}
for _, x := range xs {
    seen[x] = struct{}{}
}
```

**魔理沙**: 「種類数」「出現回数が K 以上」はカウンタ、`defaultdict` で集約する問題も多い。

```python
from collections import defaultdict

groups = defaultdict(list)
for i, x in enumerate(xs):
    groups[x].append(i)
```

---

## 2.6 文字列処理

**霊夢**: 文字列、競プロだと何が出るの？

**魔理沙**: **辞書順**、**先頭・末尾**、**1 文字ずつシフト**、**部分列** の確認だ。茶色では全探索やソートと組み合わせる。

#### 辞書順の比較

```python
s = "abc"
t = "abcd"
print(s < t)  # True（先頭から比較）
```

#### 1 文字シフト（abc152_b 相当）

→ [solutions/python/02/abc152_b/](../solutions/python/02/abc152_b/) / [solutions/go/02/abc152_b/](../solutions/go/02/abc152_b/)

アルファベットを `X` 文字シフト（`Z` の次は `A`）。

```python
import sys

def main() -> None:
    n = int(input())
    s = input().strip()
    x = int(input())
    ans = []
    for ch in s:
        if "A" <= ch <= "Z":
            base = ord("A")
            c = (ord(ch) - base + x) % 26
            ans.append(chr(base + c))
        else:
            ans.append(ch)
    print("".join(ans))

if __name__ == "__main__":
    main()
```

```go
shift := func(ch byte, x int) byte {
    if ch < 'A' || ch > 'Z' {
        return ch
    }
    c := int(ch-'A') + x
    c %= 26
    return byte('A' + c)
}
```

#### 部分文字列の存在確認（素朴）

```python
text = "abracadabra"
pat = "abra"
print(pat in text)  # True
```

---

## 2.7 座標圧縮（入門）

**霊夢**: 座標が 10 億とかで、配列に入らないって言うやつ？

**魔理沙**: **座標圧縮** は「値が大きいが、種類数は少ない」ときに、小さな番号に置き換える技法だ。ソートして重複除去し、**元の値 → 圧縮後の index** の写像を作る。

#### 手順

1. 使う値を集める  
2. ソートして重複を除く  
3. `bisect` や辞書で index を得る  

→ [solutions/python/02/compress_coords/](../solutions/python/02/compress_coords/) / [solutions/go/02/compress_coords/](../solutions/go/02/compress_coords/)

**Python**:

```python
import bisect

values = [10**9, 5, 10**9, 7, 5]
uniq = sorted(set(values))  # [5, 7, 10**9]

def compress(x: int) -> int:
    return bisect.bisect_left(uniq, x)  # 0-indexed の座標

print([compress(v) for v in values])  # [2, 0, 2, 1, 0]
```

**Go**:

```go
uniq := []int{5, 7, 1_000_000_000} // sorted unique
idx := sort.SearchInts(uniq, x)
```

**霊夢**: 何のためにつかうの？

**魔理沙**: 例えば「座標軸上のイベント」を Fenwick 木やセグ木で扱うとき、添字が小さくなる。水色以降で本番になるが、茶色では **「値が大きいが N は小さい」** と見たら思い出せ。

---

## 2.8 茶色まとめハンズオン

**霊夢**: 灰色のときより問題、長く感じる……

**魔理沙**: 型が増えただけだ。次の表を **上から** 解け。まず ABC、最後に **初めての ARC A** に挑戦する。

| 順 | 問題 ID | 想定スキル | 目安 |
|----|---------|------------|------|
| 1 | [abc142_b](https://atcoder.jp/contests/abc142/tasks/abc142_b) | ソート + 貪欲 | B（[解答](../solutions/python/02/abc142_b/)） |
| 2 | [abc152_b](https://atcoder.jp/contests/abc152/tasks/abc152_b) | 文字列シフト | B（[解答](../solutions/python/02/abc152_b/)） |
| 3 | [abc153_b](https://atcoder.jp/contests/abc153/tasks/abc153_b) | 文字列・共通部分 | B |
| 4 | [abc136_c](https://atcoder.jp/contests/abc136/tasks/abc136_c) | 貪欲・棒の切り方 | C |
| 5 | [abc146_c](https://atcoder.jp/contests/abc146/tasks/abc146_c) | 全探索・桁 | C |
| 6 | [abc151_c](https://atcoder.jp/contests/abc151/tasks/abc151_c) | グリッド・シミュレーション | C |
| 7 | [abc134_c](https://atcoder.jp/contests/abc134/tasks/abc134_c) | 累積和・条件 | C |
| 8 | [arc083_a](https://atcoder.jp/contests/arc083/tasks/arc083_a) | 三重ループ・数列（**初 ARC A**） | ARC A（[解答](../solutions/python/02/arc083_a/)） |

**ARC A 体験のコツ**

- 問題文が長くても、求めているのは **数式・ループ・ソート** のどれかだ。  
- 灰色の読み方・茶色のソート/貪欲を組み合わせろ。  
- WA したら [01.md](01.md) のデバッグ表に戻る。  

索引: [12.md](12.md)

---

## Part 2 到達チェック

- [ ] カスタム比較でソートできる（Python `key` / Go `sort.Slice`）  
- [ ] 終了時刻ソートの区間貪欲が書ける  
- [ ] 累積和で区間和を O(1) で出せる  
- [ ] 差分配列のイメージが説明できる  
- [ ] グリッド + 4 方向の移動が書ける  
- [ ] Counter / map で頻度が数えられる  
- [ ] 座標圧縮の手順を紙に書ける  
- [ ] まとめで ABC C を **2 問以上 AC**、**arc083_a** を 1 回提出した  

**霊夢**: ARC、ちょっと怖かったけど三重ループならいけるかも。

**魔理沙**: [03.md](03.md) では **BFS/DFS・二分探索・DP** が主役だ。茶色で固めたソートと累積和は、そのまま使える。進むぜ。

---

## まとめ

| 節 | 内容 |
|----|------|
| 2.1 | ソート、カスタム比較 |
| 2.2 | 貪欲（区間、コイン）、abc142_b |
| 2.3 | グリッド、4 方向、BFS の予告 |
| 2.4 | 累積和、差分配列 |
| 2.5 | Counter / map / set |
| 2.6 | 文字列、abc152_b |
| 2.7 | 座標圧縮 |
| 2.8 | ABC B〜C + arc083_a |

← [01.md](01.md) | [目次](00-toc.md) | 次 → [03.md](03.md)（緑）
