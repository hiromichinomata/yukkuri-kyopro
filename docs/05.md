# Part 5 — 青色（Blue）

**目標**: ABC の **F**、**ARC B〜C**、**AGC A** を解く。**MST・応用 DP・lazy セグ木・フロー・ゲーム・文字列・幾何** を体系化する。  
解説は **ゆっくり霊夢** と **ゆっくり魔理沙** の対談形式。

**リポジトリ上のコード（Part 5）**

| 種別 | 一覧 |
|------|------|
| Python | [solutions/python/05/](../solutions/python/05/) |
| Go | [solutions/go/05/](../solutions/go/05/) |

← [04.md](04.md)（水色） | [目次](00-toc.md) | 次 → [06.md](06.md)（黄色）

---

## 5.1 最小全域木・クラスカル

**霊夢**: [04.md](04.md) で DSU はやった。最小全域木って何？

**魔理沙**: 無向連結グラフで、**すべての頂点をつなぎ、辺の重み和が最小** になる木のことだ。クラスカルは **辺を軽い順に見て、閉路を作らなければ採用** するアルゴリズムだ。

| 手順 | 内容 |
|------|------|
| 1 | 辺を重み昇順にソート |
| 2 | 各辺 `(u,v,w)` で `unite(u,v)` が成功したら採用 |
| 3 | 採用辺の `w` を足す |

→ [solutions/python/05/kruskal/](../solutions/python/05/kruskal/) / [solutions/go/05/kruskal/](../solutions/go/05/kruskal/)（[04.md](04.md) の DSU を発展）

```python
def kruskal(n, edges):
    edges.sort(key=lambda e: e[2])
    dsu = DSU(n)
    total = 0
    used = 0
    for u, v, w in edges:
        if dsu.unite(u, v):
            total += w
            used += 1
            if used == n - 1:
                break
    return total
```

```go
sort.Slice(edges, func(i, j int) bool { return edges[i].w < edges[j].w })
// unite 成功で total += w、n-1 本取れたら完了
```

**霊夢**: Prim 法は？

**魔理沙**: 頂点から広げる方法もある。競プロでは **クラスカル + DSU** が書きやすいことが多い。

---

## 5.2 最長共通部分列・最長増加部分列（応用）

**霊夢**: LIS、緑のときは O(N²) だったよね？

**魔理沙**: 青色では **LIS を O(N log N)** にするのが定番だ。テール配列 + `bisect` だ。

#### LIS（O(N log N)）

→ [solutions/python/05/lis_fast/](../solutions/python/05/lis_fast/) / [solutions/go/05/lis_fast/](../solutions/go/05/lis_fast/)

```python
import bisect

def lis_length(a):
    tails = []
    for x in a:
        i = bisect.bisect_left(tails, x)
        if i == len(tails):
            tails.append(x)
        else:
            tails[i] = x
    return len(tails)
```

```go
// tails を維持し、lower_bound 相当で挿入位置を決める
```

#### LCS（O(NM)）

→ [solutions/python/05/lcs/](../solutions/python/05/lcs/) / [solutions/go/05/lcs/](../solutions/go/05/lcs/)

```python
def lcs(s, t):
    n, m = len(s), len(t)
    dp = [[0] * (m + 1) for _ in range(n + 1)]
    for i in range(n):
        for j in range(m):
            if s[i] == t[j]:
                dp[i + 1][j + 1] = dp[i][j] + 1
            else:
                dp[i + 1][j + 1] = max(dp[i + 1][j], dp[i][j + 1])
    return dp[n][m]
```

**魔理沙**: ARC B〜C では **LIS の長さ** や **LCS 風 DP** が、座標圧縮やセグ木とセットで出る。

---

## 5.3 セグメントツリー（応用）— lazy propagation

**霊夢**: [04.md](04.md) のセグ木は、一点更新だけだった。

**魔理沙**: **区間に +v、区間和を聞く** には **遅延セグメントツリー（lazy）** を使う。ノードに「保留中の加算値」を持たせ、push で子に伝播する。

| 操作 | 内容 |
|------|------|
| `range_add(l, r, v)` | `[l,r)` に v を加える |
| `range_sum(l, r)` | `[l,r)` の和 |

→ [solutions/python/05/segtree_lazy/](../solutions/python/05/segtree_lazy/) / [solutions/go/05/segtree_lazy/](../solutions/go/05/segtree_lazy/)

```python
class LazySegTree:
    def __init__(self, data):
        n = 1
        while n < len(data):
            n <<= 1
        self.n = n
        self.seg = [0] * (2 * n)
        self.lazy = [0] * (2 * n)

    def _push(self, i, l, r):
        if self.lazy[i] == 0:
            return
        self.seg[i] += self.lazy[i] * (r - l)
        if i < self.n:
            self.lazy[2 * i] += self.lazy[i]
            self.lazy[2 * i + 1] += self.lazy[i]
        self.lazy[i] = 0

    def range_add(self, ql, qr, x, node=1, l=0, r=None):
        if r is None:
            r = self.n
        if qr <= l or r <= ql:
            return
        if ql <= l and r <= qr:
            self.lazy[node] += x
            self._push(node, l, r)
            return
        self._push(node, l, r)
        m = (l + r) // 2
        self.range_add(ql, qr, x, 2 * node, l, m)
        self.range_add(ql, qr, x, 2 * node + 1, m, r)
        self._push(2 * node, l, m)
        self._push(2 * node + 1, m, r)
        self.seg[node] = self.seg[2 * node] + self.seg[2 * node + 1]
```

**霊夢**: 実装、長い……

**魔理沙**: テンプレを [solutions/](../solutions/python/05/segtree_lazy/) から持ち、問題に合わせて `max` や `min` に変える。ABC F / ARC C で差がつく。

---

## 5.4 フロー（入門）

**霊夢**: 最大流って、ネットワークの流れ？

**魔理沙**: 有向グラフで **源点 s から吸点 t へ、辺容量を超えずに送れる流量の最大** を求める。競プロ入門は **Ford-Fulkerson（BFS で増加路）** や **Dinic** だ。

#### 増加路 BFS（入門）

→ [solutions/python/05/max_flow/](../solutions/python/05/max_flow/) / [solutions/go/05/max_flow/](../solutions/go/05/max_flow/)

```python
from collections import deque

def max_flow(cap, s, t, n):
    flow = 0
    while True:
        parent = [-1] * n
        parent[s] = s
        q = deque([s])
        while q:
            v = q.popleft()
            for to in range(n):
                if parent[to] == -1 and cap[v][to] > 0:
                    parent[to] = v
                    q.append(to)
        if parent[t] == -1:
            break
        # 増加路のボトルネック
        f = 10**18
        v = t
        while v != s:
            f = min(f, cap[parent[v]][v])
            v = parent[v]
        v = t
        while v != s:
            u = parent[v]
            cap[u][v] -= f
            cap[v][u] += f
            v = u
        flow += f
    return flow
```

**最小カット**: 最大流 = 最小カット（s 側と t 側を分ける辺容量の和の最小）。「割り当て」「マッチング」に還元される問題が多い。

---

## 5.5 ゲーム理論・Nim

**霊夢**: 先手必勝って、どう判定するの？

**魔理沙**: **Nim** では各山の石数を **xor** した値（ニム和）が 0 なら後手必勝、そうでなければ先手必勝だ。

```python
def nim_xor(piles):
    x = 0
    for p in piles:
        x ^= p
    return "first" if x != 0 else "second"
```

#### Grundy 数（入門）

→ [solutions/python/05/grundy/](../solutions/python/05/grundy/) / [solutions/go/05/grundy/](../solutions/go/05/grundy/)

複数ゲームの合体は、各ゲームの Grundy 数を xor。 mex（最小非負整数不在）で定義する。

```python
def mex(s):
    m = 0
    while m in s:
        m += 1
    return m
```

**魔理沙**: ARC C では「ゲームを分解して xor」パターンが出る。完全把握は難しいが、**xor 思い出し** で方針が立つ。

---

## 5.6 文字列：KMP・Z アルゴリズム

**霊夢**: パターンマッチ、毎回 strcmp するの？

**魔理沙**: **KMP** は失敗関数 `next` を前計算し、テキスト走査を O(N+M) にする。

#### KMP

→ [solutions/python/05/kmp/](../solutions/python/05/kmp/) / [solutions/go/05/kmp/](../solutions/go/05/kmp/)

```python
def build_next(p):
    m = len(p)
    next_arr = [0] * m
    j = 0
    for i in range(1, m):
        while j > 0 and p[i] != p[j]:
            j = next_arr[j - 1]
        if p[i] == p[j]:
            j += 1
            next_arr[i] = j
    return next_arr

def kmp_search(text, pat):
    next_arr = build_next(pat)
    j = 0
    pos = []
    for i in range(len(text)):
        while j > 0 and text[i] != pat[j]:
            j = next_arr[j - 1]
        if text[i] == pat[j]:
            j += 1
        if j == len(pat):
            pos.append(i - len(pat) + 1)
            j = next_arr[j - 1]
    return pos
```

#### Z アルゴリズム

→ [solutions/python/05/z_algorithm/](../solutions/python/05/z_algorithm/) / [solutions/go/05/z_algorithm/](../solutions/go/05/z_algorithm/)

`Z[i]` = S と S[i:] の最長共通接頭辞長。構築 O(N)。

```python
def z_algorithm(s):
    n = len(s)
    z = [0] * n
    l = r = 0
    for i in range(1, n):
        if i <= r:
            z[i] = min(r - i + 1, z[i - l])
        while i + z[i] < n and s[z[i]] == s[i + z[i]]:
            z[i] += 1
        if i + z[i] - 1 > r:
            l, r = i, i + z[i] - 1
    return z
```

---

## 5.7 座標幾何（入門）

**霊夢**: 凸包って、ゴムで囲んだ形？

**魔理沙**: 点集合を **凸な最小の多角形** で包むものだ。まず **CCW（反時計回り判定）** を覚えろ。

#### CCW

→ [solutions/python/05/ccw/](../solutions/python/05/ccw/) / [solutions/go/05/ccw/](../solutions/go/05/ccw/)

```python
def ccw(a, b, c):
    # >0: 反時計, <0: 時計, =0: 一直線
    return (b[0] - a[0]) * (c[1] - a[1]) - (b[1] - a[1]) * (c[0] - a[0])
```

```go
func ccw(a, b, c [2]int) int {
    return (b[0]-a[0])*(c[1]-a[1]) - (b[1]-a[1])*(c[0]-a[0])
}
```

#### 凸包（Andrew の monotone chain）

→ [solutions/python/05/convex_hull/](../solutions/python/05/convex_hull/) / [solutions/go/05/convex_hull/](../solutions/go/05/convex_hull/)

```python
def convex_hull(points):
    points = sorted(set(points))
    if len(points) <= 1:
        return points
    lower = []
    for p in points:
        while len(lower) >= 2 and ccw(lower[-2], lower[-1], p) <= 0:
            lower.pop()
        lower.append(p)
    upper = []
    for p in reversed(points):
        while len(upper) >= 2 and ccw(upper[-2], upper[-1], p) <= 0:
            upper.pop()
        upper.append(p)
    return lower[:-1] + upper[:-1]
```

**魔理沙**: 線分交差は CCW の符号の組み合わせで判定する。ARC C の幾何は **実装ミス（整数 overflow）** に注意。Go は `int64` を使え。

---

## 5.8 確率・期待値 DP

**霊夢**: 確率 DP って、遷移に確率を掛けるの？

**魔理沙**: 状態 `dp[i]` を **確率** または **期待値** として更新する。

```python
# 例: サイコロでちょうど n に到達する確率（表が 1..6）
def prob_reach(n, max_face=6):
    dp = [0.0] * (n + 1)
    dp[0] = 1.0
    for i in range(n):
        for f in range(1, max_face + 1):
            if i + f <= n:
                dp[i + f] += dp[i] / max_face
    return dp[n]
```

#### 期待値

```python
# 期待値 DP: dp[i] = 状態 i からの期待ステップ数
INF = 10**18
dp = [0.0] * (n + 1)
dp[target] = 0.0
for i in range(target - 1, -1, -1):
    dp[i] = 1.0 + sum(dp[j] for j in next_states(i)) / len(next_states(i))
```

→ [solutions/python/05/probability_dp/](../solutions/python/05/probability_dp/) / [solutions/go/05/probability_dp/](../solutions/go/05/probability_dp/)

**魔理沙**: mod 世界では分数は **逆元** で扱う（[04.md](04.md)）。浮動小数は誤差に注意し、問題が許せば有理数 mod で書く。

---

## 5.9 青色まとめハンズオン

**霊夢**: ARC C と AGC A、いよいよ本番感がある……

**魔理沙**: 青色は **テンプレの実戦投入** だ。次の表を進め、使った技法をメモしろ。

| 順 | 問題 ID | 想定スキル | 目安 |
|----|---------|------------|------|
| 1 | [abc214_f](https://atcoder.jp/contests/abc214/tasks/abc214_f) | 文字列・組合せ | F |
| 2 | [abc256_d](https://atcoder.jp/contests/abc256/tasks/abc256_d) | 二分・ソート | D |
| 3 | [abc240_f](https://atcoder.jp/contests/abc240/tasks/abc240_f) | DP | F |
| 4 | [abc229_d](https://atcoder.jp/contests/abc229/tasks/abc229_d) | 尺取り・累積 | D |
| 5 | [arc146_b](https://atcoder.jp/contests/arc146/tasks/arc146_b) | LIS 応用 | ARC B |
| 6 | [arc151_b](https://atcoder.jp/contests/arc151/tasks/arc151_b) | ゲーム・xor | ARC B |
| 7 | [arc163_c](https://atcoder.jp/contests/arc163/tasks/arc163_c) | グラフ・構築 | ARC C |
| 8 | [arc170_b](https://atcoder.jp/contests/arc170/tasks/arc170_b) | 文字列・DP | ARC B |
| 9 | [agc004_a](https://atcoder.jp/contests/agc004/tasks/agc004_a) | xor 貪欲（[解答](../solutions/python/05/agc004_a/)） | AGC A |
| 10 | [agc005_a](https://atcoder.jp/contests/agc005/tasks/agc005_a) | シミュレーション | AGC A |

**AGC A を安定させるコツ**

- 問題を **小さい例** で手で追う  
- 「xor」「ソート」「貪欲」「DFS」のどれかに落とす  
- 無理なら [06.md](06.md) の高度技法を preview する  

索引: [12.md](12.md)

---

## Part 5 到達チェック

- [ ] クラスカルで MST が書ける  
- [ ] LIS を O(N log N) で書ける  
- [ ] lazy セグ木の「区間加算・区間和」を説明できる  
- [ ] 最大流の増加路 BFS が書ける（入門）  
- [ ] Nim の xor 判定ができる  
- [ ] KMP または Z を実装できる  
- [ ] CCW と凸包が書ける  
- [ ] 確率 DP の遷移式を立てられる  
- [ ] まとめで ABC F を **1 問以上 AC**、ARC C を **1 問試す**、AGC A を **2 問以上 AC**  

**霊夢**: テンプレの引き出し、増えてきたわ。

**魔理沙**: [06.md](06.md) は **重心分解・FFT・高度 DP** だ。青色のセグ木とフローがそのまま伸びる。進むぜ。

---

## まとめ

| 節 | 内容 |
|----|------|
| 5.1 | 最小全域木・クラスカル |
| 5.2 | LIS 高速、LCS |
| 5.3 | lazy セグメントツリー |
| 5.4 | 最大流入門 |
| 5.5 | Nim、Grundy |
| 5.6 | KMP、Z |
| 5.7 | CCW、凸包 |
| 5.8 | 確率・期待値 DP |
| 5.9 | ABC F + ARC + AGC A |

← [04.md](04.md) | [目次](00-toc.md) | 次 → [06.md](06.md)（黄色）
