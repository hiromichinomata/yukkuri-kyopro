# Part 1 — 灰色（Gray）

**目標**: ABC の **A〜B** を自力で解く。標準入力・ループ・全探索・簡単なシミュレーションまでを固める。  
解説は **ゆっくり霊夢** と **ゆっくり魔理沙** の対談形式。

**リポジトリ上のコード（Part 1）**

| 種別 | 一覧 |
|------|------|
| Python | [solutions/python/01/](../solutions/python/01/) |
| Go | [solutions/go/01/](../solutions/go/01/) |

← [00.md](00.md)（はじめに） | [目次](00-toc.md) | 次 → [02.md](02.md)（茶色）

---

## 1.1 標準入力・出力

**霊夢**: [00.md](00.md) で A+B はできた。でも問題文、1 行じゃないこともあるんでしょ？

**魔理沙**: その通り。灰色では **読み方のパターン** を体に覚えるのが仕事だ。AtCoder はだいたい次のどれかだ。

| パターン | 入力のイメージ | よくある問題 |
|----------|----------------|--------------|
| 1 行 1 個 | `5` | 整数 1 つ |
| 1 行 複数 | `3 1 4` | スペース区切り |
| 2 行目以降に N 行 | `3` のあと 3 行 | 配列・文字列 |

**霊夢**: Python ではどう読むの？

**魔理沙**: 基本は 3 つだ。`input()` 1 行、`split()` で分割、`int()` で数にする。

#### 1 行・複数整数（Python）

```python
import sys

def main() -> None:
    line = sys.stdin.readline()
    a, b, c = map(int, line.split())
    print(a + b + c)

if __name__ == "__main__":
    main()
```

#### 1 行・複数整数（Go）

```go
package main

import (
	"bufio"
	"fmt"
	"os"
)

func main() {
	in := bufio.NewReader(os.Stdin)
	var a, b, c int
	fmt.Fscan(in, &a, &b, &c)
	fmt.Println(a + b + c)
}
```

**霊夢**: `map(int, ...)` って何？

**魔理沙**: 文字列のリストを、まとめて整数に変換する書き方だ。競プロでは毎日出会う。

#### 先に個数 N が来る（Python）

→ [solutions/python/01/io_n_lines/main.py](../solutions/python/01/io_n_lines/main.py)

```python
import sys

def main() -> None:
    input = sys.stdin.readline
    n = int(input())
    xs = [int(input()) for _ in range(n)]
    print(sum(xs))

if __name__ == "__main__":
    main()
```

#### 先に個数 N が来る（Go）

→ [solutions/go/01/io_n_lines/main.go](../solutions/go/01/io_n_lines/main.go)

```go
package main

import (
	"bufio"
	"fmt"
	"os"
)

func main() {
	in := bufio.NewReader(os.Stdin)
	out := bufio.NewWriter(os.Stdout)
	defer out.Flush()

	var n int
	fmt.Fscan(in, &n)
	sum := 0
	for i := 0; i < n; i++ {
		var x int
		fmt.Fscan(in, &x)
		sum += x
	}
	fmt.Fprintln(out, sum)
}
```

**入力例**:

```text
3
10
20
30
```

**出力**: `60`

**霊夢**: 出力は `print` だけ？ 改行は？

**魔理沙**: `print(x)` は **末尾に改行が 1 つ** 付くのが AtCoder の想定だ。余計な文字列を足すと WA になる。

```python
# OK
print(42)

# WA になりがち
print("答えは", 42)
```

---

## 1.2 四則演算・条件分岐・ループ

**霊夢**: `if` と `for`、競プロでもそのまま使えるの？

**魔理沙**: 使える。灰色の A 問題は **条件分岐 1 段** と **ループ 0〜1 段** が多い。剰余 `%` も早めに慣れろ。「偶数か」「割り切れるか」は超頻出だ。

#### 条件分岐（Python）

```python
x = int(input())
if x % 2 == 0:
    print("even")
else:
    print("odd")
```

#### 条件分岐（Go）

```go
var x int
fmt.Fscan(in, &x)
if x%2 == 0 {
    fmt.Println("even")
} else {
    fmt.Println("odd")
}
```

#### for ループで 1 から N（Python）

```python
n = int(input())
s = 0
for i in range(1, n + 1):
    s += i
print(s)
```

#### for ループ（Go）

```go
var n int
fmt.Fscan(in, &n)
s := 0
for i := 1; i <= n; i++ {
    s += i
}
fmt.Println(s)
```

### 🖐️ ハンズオン: 3 つの最大値

→ [solutions/python/01/max_of_three/](../solutions/python/01/max_of_three/) / [solutions/go/01/max_of_three/](../solutions/go/01/max_of_three/)

1 行に 3 整数が来て、最大値を出力する（ABC A 相当の練習）。

```bash
python3 solutions/python/01/max_of_three/main.py < solutions/python/01/max_of_three/input.txt
go run ./solutions/go/01/max_of_three/ < solutions/python/01/max_of_three/input.txt
```

**Python**:

```python
import sys

def main() -> None:
    a, b, c = map(int, sys.stdin.readline().split())
    print(max(a, b, c))

if __name__ == "__main__":
    main()
```

**Go**:

```go
var a, b, c int
fmt.Fscan(in, &a, &b, &c)
ans := a
if b > ans {
    ans = b
}
if c > ans {
    ans = c
}
fmt.Println(ans)
```

### 🖐️ ハンズオン: 積が 1〜9 か（abc086_a 相当）

→ [solutions/python/01/abc086_a/](../solutions/python/01/abc086_a/) / [solutions/go/01/abc086_a/](../solutions/go/01/abc086_a/)

**霊夢**: AtCoder の問題そのものを解く練習？

**魔理沙**: だ。入出力が読めれば A は解ける例だ。

```python
import sys

def main() -> None:
    a, b = map(int, sys.stdin.readline().split())
    p = a * b
    print("Yes" if 1 <= p <= 9 else "No")

if __name__ == "__main__":
    main()
```

```go
var a, b int
fmt.Fscan(in, &a, &b)
p := a * b
if 1 <= p && p <= 9 {
    fmt.Println("Yes")
} else {
    fmt.Println("No")
}
```

---

## 1.3 配列・リスト・文字列の基礎

**霊夢**: 配列って、競プロだと何に使うの？

**魔理沙**: 「N 個の数を並べて扱う」ときの入れ物だ。Python では **リスト**、Go では **スライス** と呼ぶ。

#### リストの読み方（Python）

```python
import sys

def main() -> None:
    input = sys.stdin.readline
    n = int(input())
    xs = list(map(int, input().split()))
    print(xs[0], xs[-1], len(xs))

if __name__ == "__main__":
    main()
```

- `xs[0]` … 先頭  
- `xs[-1]` … 末尾  
- `len(xs)` … 長さ（添字は `0` から `n-1`）

#### スライス（Go）

```go
xs := make([]int, n)
for i := 0; i < n; i++ {
    fmt.Fscan(in, &xs[i])
}
fmt.Println(xs[0], xs[n-1], len(xs))
```

#### 文字列（Python）

→ [solutions/python/01/abc081_a/](../solutions/python/01/abc081_a/) / [solutions/go/01/abc081_a/](../solutions/go/01/abc081_a/)

`0` を除いた長さを数える（abc081_a 相当）。

```python
s = input().strip()
t = s.replace("0", "")
print(len(t))
```

```go
var s string
fmt.Fscan(in, &s)
cnt := 0
for _, ch := range s {
    if ch != '0' {
        cnt++
    }
}
fmt.Println(cnt)
```

**霊夢**: `replace` だけで解けちゃうの？

**魔理沙**: 灰色では **言語の標準機能で足りる** ことも多い。茶色以降は自分でループする場面が増える。

#### スライスで部分列（Python）

```python
s = "abcdef"
print(s[1:4])   # "bcd"  添字 1 以上 4 未満
print(s[::-1])  # 反転（たまに使う）
```

---

## 1.4 全探索（バイト力）

**霊夢**: アルゴリズムって言うと難しそう……

**魔理沙**: 灰色の「全探索」は **力任せで全部試す** だ。N が小さい（例: 100 以下）とき、二重ループで十分なことが多い。

**霊夢**: 二重ループって？

**魔理沙**: ループの中にもう 1 個ループを入れる。組み合わせやペアを調べるとき使う。

#### ペアの和が K になるか（Python）

→ [solutions/python/01/brute_force_pair/](../solutions/python/01/brute_force_pair/) / [solutions/go/01/brute_force_pair/](../solutions/go/01/brute_force_pair/)

```python
import sys

def main() -> None:
    input = sys.stdin.readline
    n, k = map(int, input().split())
    xs = list(map(int, input().split()))
    ok = False
    for i in range(n):
        for j in range(i + 1, n):
            if xs[i] + xs[j] == k:
                ok = True
                break
        if ok:
            break
    print("Yes" if ok else "No")

if __name__ == "__main__":
    main()
```

```go
ok := false
for i := 0; i < n; i++ {
    for j := i + 1; j < n; j++ {
        if xs[i]+xs[j] == k {
            ok = true
            break
        }
    }
    if ok {
        break
    }
}
if ok {
    fmt.Println("Yes")
} else {
    fmt.Println("No")
}
```

**霊夢**: `i + 1` から始めるのはなぜ？

**魔理沙**: 同じペアを二回数えないためだ。`(i,j)` と `(j,i)` を別に見ない。

#### 全探索の見積もり

| N の目安 | 二重ループ | 感覚 |
|----------|------------|------|
| ≤ 100 | 〜10^4 | 余裕 |
| ≤ 1,000 | 〜10^6 | ギリギリ |
| ≤ 10^5 | 〜10^10 | 無理（別アルゴリズムへ） |

**魔理沙**: 制約を読んで「何回ループするか」をざっくり数える癖をつけろ。TLE はだいたいここで起きる。

---

## 1.5 関数・スコープ・デバッグ

**霊夢**: 1 ファイルに全部書くの？ 長くならない？

**魔理沙**: 灰色では短いままでもいいが、**読みやすくするために関数に分ける** のは早めにやるとよい。

#### 関数に分ける（Python）

→ [solutions/python/01/gcd_fn/](../solutions/python/01/gcd_fn/) / [solutions/go/01/gcd_fn/](../solutions/go/01/gcd_fn/)

```python
def gcd(a: int, b: int) -> int:
    while b:
        a, b = b, a % b
    return a

def main() -> None:
    a, b = map(int, input().split())
    print(gcd(a, b))

if __name__ == "__main__":
    main()
```

```go
func gcd(a, b int) int {
    for b != 0 {
        a, b = b, a%b
    }
    return a
}
```

**霊夢**: バグったときどうするの？

**魔理沙**: ローカルでは **print デバッグ** で十分だ。提出直前に消す。

```python
import sys

def debug(*args):
    print(*args, file=sys.stderr)

def main() -> None:
    n = int(input())
    debug("n =", n)  # 提出前に削除
    print(n * 2)
```

| 症状 | よくある原因 |
|------|----------------|
| WA | 出力形式、境界（0, 1, 最大） |
| RE | 添字はみ出し、ゼロ除算、再帰深さ |
| TLE | ループ回数過多 |

---

## 1.6 模擬（シミュレーション）

**霊夢**: シミュレーションって、何をするの？

**魔理沙**: 問題文のルールどおりに **状態を更新していく** ことだ。「カードを引く」「時刻を進める」など、while / for で 1 ステップずつ再現する。

### 🖐️ ハンズオン: 先頭からカードを引く（abc088_b 相当）

→ [solutions/python/01/abc088_b/](../solutions/python/01/abc088_b/) / [solutions/go/01/abc088_b/](../solutions/go/01/abc088_b/)

**ルール（要約）**: 長さ N の数列。先手・後手が交互に、**左端か右端** のカードを 1 枚取る。合計が大きい方が勝ち。最適に取ったときの先手の合計を出力する（典型は B 問題）。

**霊夢**: 最適って、全部試すの？

**魔理沙**: abc088_b は N が小さいので全探索や DP もあるが、灰色では **「貪欲に大きい方を取る」シミュレーション** の練習として、まずはルールをコードに落とすことが大事だ。本リポジトリでは **シンプル版**（交互に左端だけ取る例）も置いてある。本番は AtCoder で問題文を読んで実装しろ。

#### 交互に左端を取る（練習・Python）

→ [solutions/python/01/simulate_left_only/](../solutions/python/01/simulate_left_only/)

```python
import sys

def main() -> None:
    input = sys.stdin.readline
    n = int(input())
    xs = list(map(int, input().split()))
    t = 0  # 先手の合計
    turn = 0
    while xs:
        t += xs.pop(0)
        turn ^= 1
    print(t)

if __name__ == "__main__":
    main()
```

**魔理沙**: 本番の abc088_b は左右の選択が要る。次の [茶色 Part](02.md) 前に、AtCoder で本題を解いてみろ。

#### abc088_b（提出用・再帰的メモ化の簡易版）

本リポジトリの [abc088_b](../solutions/python/01/abc088_b/) は、区間 `[l,r]` で先手が最適に取ったときの得点差を **メモ化再帰** で求める定番実装だ（N ≤ 100 程度なら通る）。

```python
from functools import lru_cache

@lru_cache(maxsize=None)
def f(l: int, r: int) -> int:
    if l == r:
        return a[l]
    return max(a[l] - f(l + 1, r), a[r] - f(l, r - 1))
```

**霊夢**: 難しく見える……

**魔理沙**: 灰色では「while で 1 ステップずつ」でもよい。まずは **動くシミュレーション** を書く経験を積め。

---

## 1.7 数学の基礎

**霊夢**: 数学苦手なんだけど……

**魔理沙**: 灰色で出るのは **最大公約数（GCD）**、**倍数・剰余**、**偶奇（パリティ）** くらいだ。テンプレを覚えろ。

#### GCD（ユークリッドの互除法）

→ [solutions/python/01/gcd_fn/](../solutions/python/01/gcd_fn/)（1.5 と共通）

```python
def gcd(a: int, b: int) -> int:
    while b:
        a, b = b, a % b
    return a
```

```go
func gcd(a, b int) int {
    for b != 0 {
        a, b = b, a%b
    }
    return a
}
```

#### 倍数・剰余

```python
n = int(input())
if n % 3 == 0 and n % 5 == 0:
    print("FizzBuzz")
elif n % 3 == 0:
    print("Fizz")
elif n % 5 == 0:
    print("Buzz")
else:
    print(n)
```

#### パリティ（_XOR の入門）

2 つの数が同じ偶奇なら `(a ^ b) % 2 == 0` のように書ける。灰色では「奇数個か？」のカウントで十分なことが多い。

```python
xs = list(map(int, input().split()))
odd = sum(x % 2 for x in xs)
print(odd % 2)  # 奇数個なら 1
```

---

## 1.8 灰色まとめハンズオン

**霊夢**: ここまで読んだら、何を解けば卒業？

**魔理沙**: ABC の **A を安定**、**B に挑戦** できる状態だ。次の表を上から順にやれ。問題文は AtCoder で読む（全文は転載しない）。

| 順 | 問題 ID | 想定スキル | 目安 |
|----|---------|------------|------|
| 1 | [abc200_a](https://atcoder.jp/contests/abc200/tasks/abc200_a) | 条件分岐 | A |
| 2 | [abc201_a](https://atcoder.jp/contests/abc201/tasks/abc201_a) | 比較・出力 | A |
| 3 | [abc214_a](https://atcoder.jp/contests/abc214/tasks/abc214_a) | 分岐 | A |
| 4 | [abc086_a](https://atcoder.jp/contests/abc086/tasks/abc086_a) | 積・範囲 | A（本書: [abc086_a](../solutions/python/01/abc086_a/)） |
| 5 | [abc081_a](https://atcoder.jp/contests/abc081/tasks/abc081_a) | 文字列 | A（本書: [abc081_a](../solutions/python/01/abc081_a/)） |
| 6 | [abc215_a](https://atcoder.jp/contests/abc215/tasks/abc215_a) | 文字 1 つ | A |
| 7 | [abc088_b](https://atcoder.jp/contests/abc088/tasks/abc088_b) | シミュレーション / DP | B（本書: [abc088_b](../solutions/python/01/abc088_b/)） |
| 8 | [abc085_b](https://atcoder.jp/contests/abc085/tasks/abc085_b) | 全探索・ペア | B |

**進め方**

1. 紙に「入力 → 処理 → 出力」を書く  
2. サンプルでローカル実行  
3. 提出 → WA なら表のデバッグを見る  
4. AC したら **1 週間後にもう一度** 手で書く  

索引は [12.md](12.md) にも登録してある。

---

## Part 1 到達チェック

- [ ] 1 行 / 複数整数 / N 行の読み分けができる  
- [ ] `if`・`for`・`%` で A 相当が書ける  
- [ ] リスト（スライス）の先頭・末尾・長さが扱える  
- [ ] 二重ループの全探索が書け、N が小さい理由を説明できる  
- [ ] 簡単なシミュレーションを while で書ける  
- [ ] まとめの A 問題を **3 問以上 AC**、B を **1 問以上** 試した  

**霊夢**: 灰色、卒業できそう！ 次は茶色？

**魔理沙**: [02.md](02.md) では **ソート・貪欲・累積和** だ。テンプレは [00.md](00.md) のまま使える。入力の型だけ増やしていくぜ。

---

## まとめ

| 節 | 内容 |
|----|------|
| 1.1 | 標準入力のパターン、出力形式 |
| 1.2 | 条件・ループ・剰余、A 相当ハンズオン |
| 1.3 | リスト・文字列 |
| 1.4 | 二重ループの全探索 |
| 1.5 | 関数化・デバッグ |
| 1.6 | シミュレーション |
| 1.7 | GCD・倍数・パリティ |
| 1.8 | ABC 過去問セット |

← [00.md](00.md) | [目次](00-toc.md) | 次 → [02.md](02.md)（茶色）
