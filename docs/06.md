# Part 6 — 黄色（Yellow）

**目標**: **ARC C〜D** と **AGC A〜B** を解く。**高度 DP・LCA/重心分解・Mo・NTT・フロー応用・文字列高度・組合せ** を融合する。  
解説は **ゆっくり霊夢** と **ゆっくり魔理沙** の対談形式。

**リポジトリ上のコード（Part 6）**

| 種別 | 一覧 |
|------|------|
| Python | [solutions/python/06/](../solutions/python/06/) |
| Go | [solutions/go/06/](../solutions/go/06/) |

← [05.md](05.md)（青色） | [目次](00-toc.md) | 次 → [07.md](07.md)（橙色）

---

## 6.1 高度な DP

**霊夢**: [05.md](05.md) までで DP はたくさんやった。黄色では何が増えるの？

**魔理沙**: **遷移が線形** なら **行列のべき乗**、**Convex Hull Trick（CHT）** で DP を高速化するパターンが増える。黄色では「DP の式を立てたあと、計算が重い」ときに **定数倍を log に落とす** 発想が増える。

**霊夢**: 線形漸化って、前の項の線形結合で次が決まるやつ？

**魔理沙**: その通り。フィボナッチや、タイルの敷き詰め数などが典型だ。ステップ数 n が 10^18 級だと、ループ DP は無理。**状態を k 個に圧縮して行列にする** と、O(k³ log n) で済む。

### 行列のべき乗（線形漸化）

`dp[k] = sum(c[j] * dp[k-j])` のような式は、状態をベクトルにして **遷移行列 T** を作り、`T^n` で k ステップ進める。

#### イメージ / 手順

1. 直近 k 個の `dp` を成分にした列ベクトル **v** を用意する  
2. 1 ステップ進める行列 **T** を係数から組み立てる（例: フィボなら 2×2）  
3. 答えは **v の n ステップ後** → `T^n × v` の必要成分  
4. `mat_pow(T, n)` は **二分累乗**（[02.md](02.md) の累乗と同型）

#### いつ使うか

| キーワード | 候補 |
|------------|------|
| 第 n 項、n ステップ後、10^18 回遷移 | 行列のべき乗 |
| 線形漸化、前 k 項の和で次 | 同上 |
| タイル・経路数が指数級の n | 状態次元を小さく保てるか確認 |
| 遷移が min/max で非線形 | CHT / 別 DP（下記） |

#### 計算量

| 処理 | 目安 |
|------|------|
| 行列 1 回の積 | O(k³)（k = 状態次元） |
| n ステップ | O(k³ log n) |
| フィボナッチ（k=2） | O(log n) |

#### つまずき

- **初期状態と n の対応**: `T^n` を `n-1` 回か `n` 回か、0 項の定義でズレやすい  
- **mod の位置**: 積のたびに `% mod`（オーバーフロー対策）  
- **状態次元 k**: k が大きいと k³ が重い。問題ごとに k を最小化する  
- **非線形遷移** を無理に行列化しない（min/max DP は CHT 側）

→ [solutions/python/06/matrix_pow/](../solutions/python/06/matrix_pow/) / [solutions/go/06/matrix_pow/](../solutions/go/06/matrix_pow/)

```python
def mat_mul(A, B, mod):
    n, m, p = len(A), len(B), len(B[0])
    C = [[0] * p for _ in range(n)]
    for i in range(n):
        for k in range(m):
            if A[i][k] == 0:
                continue
            for j in range(p):
                C[i][j] = (C[i][j] + A[i][k] * B[k][j]) % mod
    return C

def mat_pow(M, e, mod):
    n = len(M)
    res = [[1 if i == j else 0 for j in range(n)] for i in range(n)]
    while e:
        if e & 1:
            res = mat_mul(res, M, mod)
        M = mat_mul(M, M, mod)
        e >>= 1
    return res
```

**例: フィボナッチ第 n 項（mod）**

```python
def fib(n, mod=10**9 + 7):
    if n == 0:
        return 0
    T = [[1, 1], [1, 0]]
    Tn = mat_pow(T, n - 1, mod)
    return Tn[0][0]
```

```go
const mod = 1_000_000_007

func matMul(A, B [][]int) [][]int {
	n, m, p := len(A), len(B), len(B[0])
	C := make([][]int, n)
	for i := range C {
		C[i] = make([]int, p)
	}
	for i := 0; i < n; i++ {
		for k := 0; k < m; k++ {
			if A[i][k] == 0 {
				continue
			}
			for j := 0; j < p; j++ {
				C[i][j] = (C[i][j] + A[i][k]*B[k][j]) % mod
			}
		}
	}
	return C
}

func matPow(M [][]int, e int) [][]int {
	n := len(M)
	res := make([][]int, n)
	for i := 0; i < n; i++ {
		res[i] = make([]int, n)
		res[i][i] = 1
	}
	for e > 0 {
		if e&1 == 1 {
			res = matMul(res, M)
		}
		M = matMul(M, M)
		e >>= 1
	}
	return res
}

func fib(n int) int {
	if n == 0 {
		return 0
	}
	T := [][]int{{1, 1}, {1, 0}}
	Tn := matPow(T, n-1)
	return Tn[0][0]
}
```

### Convex Hull Trick（入門）

**魔理沙**: DP が `dp[i] = min_j (dp[j] + cost(j, i))` で、`cost` が **傾きが単調** なら、deque で直線群の下包絡を維持して O(N) になる。

**霊夢**: 直線の束の「下側の折れ線」ってこと？

**魔理沙**: 各 j が `y = m*x + b` の直線を表すと、各 i で **最小値を取る j** を探す DP だ。傾き m が単調なら、古い直線は二度と最小にならないので deque で捨てられる。

#### イメージ / 手順

1. 各候補 j を直線 `(m, b)` に変換（`cost(j,i)` の形から m, b を読む）  
2. 左から i を増やし、deque の先頭がその x=i で最小になるよう維持  
3. 新しい直線を入れるとき、**交点の順序** がおかしい後ろを pop（`bad` 判定）  
4. クエリ x（= i）で deque 先頭を評価

#### いつ使うか

| キーワード | 候補 |
|------------|------|
| `min_j (dp[j] + a[j]*x + b[j])`、傾き単調 | CHT（deque） |
| 凸コスト、区間 min | CHT または Li Chao |
| 傾きが単調でない | Li Chao 木、セグ木 |
| n, k が 10^18 | 行列べき乗の方を先に疑う |

#### 計算量

| 手法 | 目安 |
|------|------|
| ナイーブ DP | O(N²) |
| CHT（単調傾き） | O(N) |
| Li Chao | O(N log V) 程度 |

#### つまずき

- **min か max か** で deque の pop 条件が反転する  
- **x の定義**（0-index / 1-index）で直線の傾き・切片が変わる  
- 傾きが単調でないのに CHT だけ使って WA  
- `bad` の式は整数除算の丸めに注意（浮動小数は避ける）

→ [solutions/python/06/cht/](../solutions/python/06/cht/) / [solutions/go/06/cht/](../solutions/go/06/cht/)

```python
from collections import deque

def cht_min(lines):
    """lines: (slope m, intercept b) for y = m*x + b"""
    dq = deque()
    def bad(l1, l2, l3):
        return (l2[1] - l1[1]) * (l3[0] - l2[0]) >= (l2[0] - l1[0]) * (l3[1] - l2[1])
    for ln in lines:
        while len(dq) >= 2 and bad(dq[-2], dq[-1], ln):
            dq.pop()
        dq.append(ln)
    return dq
```

**霊夢**: 傾きが単調じゃないと？

**魔理沙**: 別の CHT や Li Chao 木（セグ木）を使う。黄色では **単調傾き** の型を見抜くところまででよい。

```go
type line struct{ m, b int }

func chtMin(lines []line) []line {
	dq := []line{}
	bad := func(l1, l2, l3 line) bool {
		return (l2.b-l1.b)*(l3.m-l2.m) >= (l2.m-l1.m)*(l3.b-l2.b)
	}
	for _, ln := range lines {
		for len(dq) >= 2 && bad(dq[len(dq)-2], dq[len(dq)-1], ln) {
			dq = dq[:len(dq)-1]
		}
		dq = append(dq, ln)
	}
	return dq
}
```

---

## 6.2 木の重心分解・LCA

**霊夢**: 木の「真ん中」って、重心？

**魔理沙**: **重心** は、どの部分木を切っても残りのサイズが n/2 以下になる頂点だ。**重心分解** は、重心で木を分割し、パスクエリを O(log n) 段で処理する技法だ。

**霊夢**: LCA は「最近共通の祖先」だよね。黄色ではどっちが先？

**魔理沙**: **LCA を書ける** のが先。**重心分解** は「パス上・距離 d 以内」の集計に効く。どちらも [05.md](05.md) の DFS・木の基礎の上に乗る。

### 重心分解のイメージ / 手順

1. 部分木サイズを DFS で求め、**重心**（切っても各片 ≤ n/2）を 1 つ取る  
2. その重心を根に見立て、全頂点までの距離などを集計  
3. 重心を除いた各連結成分に **再帰**（深さ O(log n)）  
4. 「距離 d 以内に〜」系のクエリで、重心を跨ぐ経路をまとめて数える

### いつ使うか

| キーワード | 候補 |
|------------|------|
| 木、2 頂点のパス・距離 | LCA |
| パス上の和・最大（静的） | LCA + 前計算、または HLD（[07.md](07.md)） |
| 距離 k 以内、部分木、根からの距離 | 重心分解 |
| 動的 link/cut | リンクカット木（橙色） |

### 計算量

| 技法 | 前処理 | クエリ |
|------|--------|--------|
| LCA（ダブリング） | O(N log N) | O(log N) |
| 重心分解（典型） | 構築 O(N log N) | 問題による |

### つまずき

- **LOG = log₂(N)+2** を忘れると祖先が足りない  
- LCA で **深い方を先に上げる**  
- 重心分解で **除去した重心を二重カウント**  
- 無向木で `parent` の向きを DFS 前に固定する

### LCA（ダブリング）

→ [solutions/python/06/lca/](../solutions/python/06/lca/) / [solutions/go/06/lca/](../solutions/go/06/lca/)

```python
import sys

sys.setrecursionlimit(10**7)

def dfs(v, p, depth, up, g, LOG):
    up[v][0] = p
    for i in range(1, LOG):
        up[v][i] = up[up[v][i - 1]][i - 1] if up[v][i - 1] != -1 else -1
    for to in g[v]:
        if to == p:
            continue
        depth[to] = depth[v] + 1
        dfs(to, v, depth, up, g, LOG)

def lca(a, b, depth, up, LOG):
    if depth[a] < depth[b]:
        a, b = b, a
    d = depth[a] - depth[b]
    for i in range(LOG):
        if d >> i & 1:
            a = up[a][i]
    if a == b:
        return a
    for i in range(LOG - 1, -1, -1):
        if up[a][i] != up[b][i]:
            a = up[a][i]
            b = up[b][i]
    return up[a][0]
```

```go
func lcaMain() {
	n := 7
	g := make([][]int, n)
	edges := [][2]int{{0, 1}, {0, 2}, {1, 3}, {1, 4}, {2, 5}, {2, 6}}
	for _, e := range edges {
		u, v := e[0], e[1]
		g[u] = append(g[u], v)
		g[v] = append(g[v], u)
	}
	const LOG = 4
	up := make([][]int, n)
	depth := make([]int, n)
	for i := range up {
		up[i] = make([]int, LOG)
		for j := range up[i] {
			up[i][j] = -1
		}
	}
	var dfs func(v, p int)
	dfs = func(v, p int) {
		up[v][0] = p
		for i := 1; i < LOG; i++ {
			if up[v][i-1] != -1 {
				up[v][i] = up[up[v][i-1]][i-1]
			}
		}
		for _, to := range g[v] {
			if to == p {
				continue
			}
			depth[to] = depth[v] + 1
			dfs(to, v)
		}
	}
	dfs(0, -1)
	lca := func(a, b int) int {
		if depth[a] < depth[b] {
			a, b = b, a
		}
		d := depth[a] - depth[b]
		for i := 0; i < LOG; i++ {
			if d>>i&1 == 1 {
				a = up[a][i]
			}
		}
		if a == b {
			return a
		}
		for i := LOG - 1; i >= 0; i-- {
			if up[a][i] != up[b][i] {
				a = up[a][i]
				b = up[b][i]
			}
		}
		return up[a][0]
	}
	_ = lca(3, 5)
}
```

**霊夢**: パス上の和や最大値は？

**魔理沙**: 重心分解 + データ構造、または **HLD（重軽分解）**（[07.md](07.md) で詳しくする）。黄色では **LCA が書ける** ことが先だ。

**霊夢**: `up[v][0] = -1` で根の親は？

**魔理沙**: 根の親を -1 にしておくと、「上に上がりすぎ」判定が簡単になる。ダブリングの初期化で `up[v][i] = -1` を埋めておくのも同じ理由だ。

---

## 6.3 平方分割

**霊夢**: クエリがたくさん来る配列問題、全部セグ木？

**魔理沙**: **平方分割** や **Mo's algorithm** で、オフラインクエリをまとめて処理する。区間 `[l,r]` のクエリを、**左端の動きが O(√N)** になるよう並べ替える。

**霊夢**: オンラインじゃダメなの？

**魔理沙**: Mo は **クエリの順番を自分で決められる** 前提。区間の add/remove が O(1)（または軽い log）なら、並べ替えで総移動量を抑えられる。セグ木で毎クエリ O(log N) でもよいが、オフラインでまとめられるなら Mo が勝つことがある。

### 平方分割のイメージ

配列を **√N 個ずつのブロック** に分け、ブロック単位の集計 + 端のブルートフォース。セグ木より実装が軽いことがある。

### Mo's algorithm のイメージ / 手順

1. ブロック幅 `B ≈ √N` を決める  
2. クエリを `(ブロック番号, 右端 r, 左端 l, 元の順番)` でソート（奇数ブロックは r 降順で **ジグザグ**）  
3. 現在区間 `[cur_l, cur_r)` を、次のクエリに合わせて **add/remove** で伸縮  
4. 各クエリの答えを記録して、元の順に戻す

### いつ使うか

| キーワード | 候補 |
|------------|------|
| 区間クエリが大量、オフライン可 | Mo / 平方分割 |
| 区間の種類数・出現回数・xor 和 | Mo（add/remove が軽いとき） |
| 区間の min/max、和の更新もある | セグ木・フェニック |
| クエリ順が固定（オンライン） | セグ木 |

### 計算量

| 手法 | 目安 |
|------|------|
| Mo（add/remove O(1)） | O((N+Q)√N) |
| ブロック平方分割 | O(N√N) 前処理 + クエリによる |

### つまずき

- **区間が `[l,r]` 閉区間か `[l,r)` か** で add/remove のループがズレる  
- 右端・左端の伸縮 **4 本の while** の順序をテンプレ通りに  
- 奇数ブロックの **r 降順** を忘れると遅くなる  
- distinct カウントで `cnt[x]==0` のときだけ増減

### Mo's algorithm（入門）

→ [solutions/python/06/mo/](../solutions/python/06/mo/) / [solutions/go/06/mo/](../solutions/go/06/mo/)

```python
def mo_order(queries, n, block):
    ordered = []
    for idx, (l, r) in enumerate(queries):
        block_id = l // block
        ordered.append((block_id, r if block_id % 2 else -r, l, r, idx))
    ordered.sort()
    return ordered

def mo_count_distinct(a, queries):
    n = len(a)
    block = int(n**0.5) + 1
    order = mo_order(queries, n, block)
    cnt = {}
    cur_l, cur_r = 0, 0
    distinct = 0
    ans = [0] * len(queries)

    def add(i):
        nonlocal distinct
        x = a[i]
        cnt[x] = cnt.get(x, 0) + 1
        if cnt[x] == 1:
            distinct += 1

    def remove(i):
        nonlocal distinct
        x = a[i]
        cnt[x] -= 1
        if cnt[x] == 0:
            distinct -= 1

    for _, _, l, r, idx in order:
        while cur_r <= r:
            add(cur_r)
            cur_r += 1
        while cur_r - 1 > r:
            cur_r -= 1
            remove(cur_r)
        while cur_l > l:
            cur_l -= 1
            add(cur_l)
        while cur_l < l:
            remove(cur_l)
            cur_l += 1
        ans[idx] = distinct
    return ans
```

**魔理沙**: 計算量はだいたい **O((N+Q)√N)**。オフラインで区間の種類数・頻度などに使う。

```go
func moCountDistinct(a []int, queries [][2]int) []int {
	n := len(a)
	block := int(math.Sqrt(float64(n))) + 1
	type item struct{ bid, r, l, idx int }
	items := make([]item, len(queries))
	for i, q := range queries {
		bid := q[0] / block
		r := q[1]
		if bid%2 == 1 {
			r = -r
		}
		items[i] = item{bid, r, q[0], i}
	}
	sort.Slice(items, func(i, j int) bool {
		if items[i].bid != items[j].bid {
			return items[i].bid < items[j].bid
		}
		return items[i].r < items[j].r
	})
	cnt := map[int]int{}
	curL, curR, distinct := 0, 0, 0
	ans := make([]int, len(queries))
	add := func(i int) {
		x := a[i]
		cnt[x]++
		if cnt[x] == 1 {
			distinct++
		}
	}
	remove := func(i int) {
		x := a[i]
		cnt[x]--
		if cnt[x] == 0 {
			distinct--
		}
	}
	for _, it := range items {
		l, r := it.l, queries[it.idx][1]
		for curR <= r {
			add(curR)
			curR++
		}
		for curR-1 > r {
			curR--
			remove(curR)
		}
		for curL > l {
			curL--
			add(curL)
		}
		for curL < l {
			remove(curL)
			curL++
		}
		ans[it.idx] = distinct
	}
	return ans
}
```

---

## 6.4 FFT・NTT（入門）

**霊夢**: 畳み込みって、多項式の掛け算？

**魔理沙**: 配列 `a`, `b` の畳み込み `c` は `c[k] = sum_i a[i]*b[k-i]`。ナイーブは O(N²)。**NTT** は mod 上で O(N log N) だ。競プロでは **998244353** と原根 3 が定番。

**霊夢**: なんで 998244353 なの？

**魔理沙**: `2^23 × 119 × 1` の形で、長さ n が 2 の冪の NTT に向いた **素数** だ。原根 3 で単位根が作れる。問題の mod が違うときは、CRT で別 mod の結果を合成する話になる（橙色以降）。

### 畳み込みのイメージ / 手順

1. 長さを **2 の冪** にパディング（畳み込み結果の長さまで）  
2. 両配列に **NTT**（順変換）  
3. 同じ位置の係数を **掛け合わせる**  
4. **逆 NTT**（逆変換）で係数列に戻す  
5. 必要な長さだけ切り出す

### いつ使うか

| キーワード | 候補 |
|------------|------|
| 畳み込み、多項式の積、係数列 | NTT / FFT |
| 組み合わせ数列の生成関数 | NTT |
| mod が 998244353 または NTT 向き素数 | NTT |
| 実数・浮動小数が必要 | FFT（誤差注意） |

### 計算量

| 手法 | 目安 |
|------|------|
| ナイーブ畳み込み | O(N²) |
| NTT / FFT | O(N log N) |

### つまずき

- 長さが **2 の冪でない** まま NTT する  
- 逆変換後に **1/n を掛け忘れ**（mod では逆元）  
- `invert=True` のとき **原根の逆** を使う  
- mod が NTT 非対応なのにそのまま NTT する

### NTT（入門・再帰）

→ [solutions/python/06/ntt/](../solutions/python/06/ntt/) / [solutions/go/06/ntt/](../solutions/go/06/ntt/)

```python
MOD = 998244353
G = 3

def ntt(a, invert=False):
    n = len(a)
    j = 0
    for i in range(1, n):
        bit = n >> 1
        while j & bit:
            j ^= bit
            bit >>= 1
        j ^= bit
        if i < j:
            a[i], a[j] = a[j], a[i]
    length = 2
    while length <= n:
        w = pow(G, (MOD - 1) // length, MOD)
        if invert:
            w = pow(w, MOD - 2, MOD)
        for i in range(0, n, length):
            for j in range(length // 2):
                u = a[i + j]
                v = a[i + j + length // 2] * w % MOD
                a[i + j] = (u + v) % MOD
                a[i + j + length // 2] = (u - v) % MOD
        length <<= 1
    if invert:
        inv_n = pow(n, MOD - 2, MOD)
        for i in range(n):
            a[i] = a[i] * inv_n % MOD
    return a

def convolution(a, b):
    n1, n2 = len(a), len(b)
    n = 1
    while n < n1 + n2 - 1:
        n <<= 1
    fa = a + [0] * (n - n1)
    fb = b + [0] * (n - n2)
    fa = ntt(fa)
    fb = ntt(fb)
    for i in range(n):
        fa[i] = fa[i] * fb[i] % MOD
    return ntt(fa, invert=True)[: n1 + n2 - 1]
```

```go
const modNTT = 998244353
const gNTT = 3

func powNTT(a, e int) int {
	res := 1
	for e > 0 {
		if e&1 == 1 {
			res = res * a % modNTT
		}
		a = a * a % modNTT
		e >>= 1
	}
	return res
}

func nttGo(a []int, invert bool) []int {
	n := len(a)
	j := 0
	for i := 1; i < n; i++ {
		bit := n >> 1
		for j&bit != 0 {
			j ^= bit
			bit >>= 1
		}
		j ^= bit
		if i < j {
			a[i], a[j] = a[j], a[i]
		}
	}
	for length := 2; length <= n; length <<= 1 {
		w := powNTT(gNTT, (modNTT-1)/length)
		if invert {
			w = powNTT(w, modNTT-2)
		}
		for i := 0; i < n; i += length {
			half := length / 2
			for j := 0; j < half; j++ {
				u := a[i+j]
				v := a[i+j+half] * w % modNTT
				a[i+j] = (u + v) % modNTT
				a[i+j+half] = (u - v + modNTT) % modNTT
			}
		}
	}
	if invert {
		invN := powNTT(n, modNTT-2)
		for i := range a {
			a[i] = a[i] * invN % modNTT
		}
	}
	return a
}

func convolutionGo(a, b []int) []int {
	n1, n2 := len(a), len(b)
	n := 1
	for n < n1+n2-1 {
		n <<= 1
	}
	fa := make([]int, n)
	fb := make([]int, n)
	copy(fa, a)
	copy(fb, b)
	fa = nttGo(fa, false)
	fb = nttGo(fb, false)
	for i := 0; i < n; i++ {
		fa[i] = fa[i] * fb[i] % modNTT
	}
	fa = nttGo(fa, true)
	return fa[:n1+n2-1]
}
```

**霊夢**: FFT は？

**魔理沙**: 実数の FFT も同型だが、競プロでは **NTT + 整数 mod** が主流。AGC B で多項式・畳み込みが出る。

---

## 6.5 最大流・最小費用流（応用）

**霊夢**: [05.md](05.md) の最大流、まだ入門だったよね？

**魔理沙**: 黄色では **Dinic** や **最小費用流** が実戦投入される。**マッチング → フロー** の還元を覚えろ。

**霊夢**: [05.md](05.md) の Ford-Fulkerson から何が変わるの？

**魔理沙**: **Dinic** は BFS で **level グラフ** を作り、DFS で **ブロッキングフロー** を一気に流す。辺の数が多いグラフでも実用的になりやすい。最小費用流は「1 単位流すたびのコスト最小」で、マッチングの **重み付き** 版にも使える。

### フロー還元のイメージ / 手順

1. 問題を **s から t へ流す** 形に言い換える  
2. 制約を **容量** に落とす（頂点容量は in/out 分割）  
3. `max_flow(s,t)` が答え、またはその値で二分  
4. 最小費用なら **費用付き辺** + ポテンシャル（または SPFA）

| 問題 | 還元のイメージ |
|------|----------------|
| 二部マッチング | s→左 容量1、左→右 容量1、右→t 容量1 |
| 頂点容量 | 頂点 v を v_in → v_out に分割、辺は v_out へ |
| 最小カット | 最大流 = 最小カット（弱・強対偶） |
| 最小費用流 | 費用付き辺で流量を送る |

### いつ使うか

| キーワード | 候補 |
|------------|------|
| マッチング、割当、選び方 | フロー |
| 最大流、容量、パイプ | Dinic |
| コスト最小、費用 | 最小費用流 |
| 頂点あたり 1 回まで | 頂点分割 + 容量 1 |

### 計算量

| 手法 | 目安 |
|------|------|
| Dinic（一般） | O(V²E) 程度（実用上はもっと速いことが多い） |
| 最小費用流 | 実装による（Dijkstra + ポテンシャルが定番） |

### つまずき

- **逆向き辺**（残量）の `rev` インデックスを間違える  
- 頂点容量を **分割せず** 辺だけに容量を付ける  
- `level[s] < 0` で終了するのに DFS を回し続ける  
- 最小費用流で **負サイクル** とポテンシャルの初期化

→ [solutions/python/06/dinic/](../solutions/python/06/dinic/) / [solutions/go/06/dinic/](../solutions/go/06/dinic/)

```python
# Dinic: BFS で level、DFS で増加路（ブロッキングフロー）
class Dinic:
    def __init__(self, n):
        self.n = n
        self.g = [[] for _ in range(n)]

    def add_edge(self, fr, to, cap):
        self.g[fr].append([to, cap, len(self.g[to])])
        self.g[to].append([fr, 0, len(self.g[fr]) - 1])

    def max_flow(self, s, t):
        # level, iter, bfs, dfs を実装（テンプレは solutions 参照）
        ...
```

**魔理沙**: テンプレは長いので [solutions/](../solutions/python/06/dinic/) をコピーして使え。最小費用流は SPFA または Dijkstra + ポテンシャルで実装する。

```go
type edge struct{ to, cap, rev int }

type Dinic struct {
	n int
	g [][]edge
}

func (d *Dinic) AddEdge(fr, to, cap int) {
	d.g[fr] = append(d.g[fr], edge{to, cap, len(d.g[to])})
	d.g[to] = append(d.g[to], edge{fr, 0, len(d.g[fr]) - 1})
}

func (d *Dinic) bfs(s, t int, level []int) bool {
	for i := range level {
		level[i] = -1
	}
	level[s] = 0
	q := []int{s}
	for head := 0; head < len(q); head++ {
		v := q[head]
		for _, e := range d.g[v] {
			if e.cap > 0 && level[e.to] < 0 {
				level[e.to] = level[v] + 1
				q = append(q, e.to)
			}
		}
	}
	return level[t] >= 0
}

func (d *Dinic) dfs(v, t, f int, level, iter []int) int {
	if v == t {
		return f
	}
	for i := iter[v]; i < len(d.g[v]); i++ {
		iter[v] = i
		e := d.g[v][i]
		if e.cap > 0 && level[v] < level[e.to] {
			df := d.dfs(e.to, t, min(f, e.cap), level, iter)
			if df > 0 {
				d.g[v][i].cap -= df
				d.g[e.to][e.rev].cap += df
				return df
			}
		}
	}
	return 0
}

func (d *Dinic) MaxFlow(s, t int) int {
	flow := 0
	level := make([]int, d.n)
	for d.bfs(s, t, level) {
		iter := make([]int, d.n)
		for {
			f := d.dfs(s, t, 1<<30, level, iter)
			if f == 0 {
				break
			}
			flow += f
		}
	}
	return flow
}
```

---

## 6.6 文字列：サフィックス配列・自動機

**霊夢**: サフィックス配列って、全部の後ろの並びをソートした感じ？

**魔理沙**: **辞書順にサフィックスを並べた配列 SA** と、**LCP（最長共通接頭辞）** の組で、部分文字列・繰り返しを扱う。

**霊夢**: 全部の後ろをソートするって、重くない？

**魔理沙**: 素朴に O(N² log N) だが、**倍々で rank を更新** すれば O(N log² N) や O(N log N) まで落とせる。黄色では簡易構築 + **用途の理解** が先だ。

### サフィックス配列のイメージ / 手順

1. 各開始位置 `i` を **サフィックス** `s[i:]` とみなす  
2. 辞書順に並べた順序が **SA**  
3. 隣接サフィックスの **LCP** 配列で「共通接頭辞の長さ」が分かる  
4. 部分文字列の出現回数・最長繰り返しなどに還元

### いつ使うか

| キーワード | 候補 |
|------------|------|
| 部分文字列、繰り返し、周期 | SA + LCP |
| 辞書順 k 番目のサフィックス | SA |
| 複数パターンの同時マッチ | Aho-Corasick |
| 1 パターンのマッチ | KMP（[05.md](05.md)） |

### 計算量

| 構築 | 目安 |
|------|------|
| 簡易 SA（本節） | O(N log² N) |
| SA-IS 等 | O(N) |

### つまずき

- 文字列末尾に **`$` などの番兵** を付けないと LCP が崩れることがある  
- `rank[i+k]` が範囲外のとき **-1** 扱いを忘れる  
- SA だけ作って **LCP を使わない**（問題によっては LCP が本丸）

### サフィックス配列（簡易構築 O(N log² N)）

→ [solutions/python/06/suffix_array/](../solutions/python/06/suffix_array/) / [solutions/go/06/suffix_array/](../solutions/go/06/suffix_array/)

```python
def suffix_array(s):
    n = len(s)
    sa = list(range(n))
    rank = [ord(c) for c in s]
    k = 1
    while k < n:
        sa.sort(key=lambda i: (rank[i], rank[i + k] if i + k < n else -1))
        new_rank = [0] * n
        new_rank[sa[0]] = 0
        for i in range(1, n):
            a = (rank[sa[i]], rank[sa[i] + k] if sa[i] + k < n else -1)
            b = (rank[sa[i - 1]], rank[sa[i - 1] + k] if sa[i - 1] + k < n else -1)
            new_rank[sa[i]] = new_rank[sa[i - 1]] + (a < b)
        rank = new_rank
        if rank[sa[-1]] == n - 1:
            break
        k <<= 1
    return sa
```

```go
func suffixArrayGo(s string) []int {
	n := len(s)
	sa := make([]int, n)
	for i := range sa {
		sa[i] = i
	}
	rank := make([]int, n)
	for i, c := range s {
		rank[i] = int(c)
	}
	for k := 1; k < n; k <<= 1 {
		sort.Slice(sa, func(i, j int) bool {
			ri, rj := rank[sa[i]], rank[sa[j]]
			if ri != rj {
				return ri < rj
			}
			ai, aj := -1, -1
			if sa[i]+k < n {
				ai = rank[sa[i]+k]
			}
			if sa[j]+k < n {
				aj = rank[sa[j]+k]
			}
			return ai < aj
		})
		newRank := make([]int, n)
		newRank[sa[0]] = 0
		for i := 1; i < n; i++ {
			prev, cur := sa[i-1], sa[i]
			a1, a2 := rank[prev], rank[cur]
			b1, b2 := -1, -1
			if prev+k < n {
				b1 = rank[prev+k]
			}
			if cur+k < n {
				b2 = rank[cur+k]
			}
			less := a1 < a2 || (a1 == a2 && b1 < b2)
			newRank[cur] = newRank[prev]
			if less {
				newRank[cur]++
			}
		}
		rank = newRank
		if rank[sa[n-1]] == n-1 {
			break
		}
	}
	return sa
}
```

### Aho-Corasick（複数パターン）

**魔理沙**: 辞書の複数文字列を同時にマッチする **オートマトン**。トライ木 + **失敗リンク（failure link）** で、ミスマッチ時も別パターンへ飛べる。

**霊夢**: KMP の「失敗時に戻る先」を木版にした感じ？

**魔理沙**: そのイメージでよい。構築は `add` で辞書を挿入 → `build` で BFS → `search` で 1 パス走査。

#### いつ使うか

| キーワード | 候補 |
|------------|------|
| 複数キーワード、辞書、禁止語 | Aho-Corasick |
| 出現位置の列挙 | AC + 終端マーク |
| パターン 1 個 | KMP / Z |

→ [solutions/python/06/aho_corasick/](../solutions/python/06/aho_corasick/) / [solutions/go/06/aho_corasick/](../solutions/go/06/aho_corasick/)

```python
from collections import deque

class AhoCorasick:
    def __init__(self) -> None:
        self.next = [{}]
        self.fail = [0]

    def add(self, s: str) -> None:
        node = 0
        for ch in s:
            if ch not in self.next[node]:
                self.next[node][ch] = len(self.next)
                self.next.append({})
                self.fail.append(0)
            node = self.next[node][ch]

    def build(self) -> None:
        q = deque()
        for ch, nxt in self.next[0].items():
            q.append(nxt)
        while q:
            v = q.popleft()
            for ch, nxt in self.next[v].items():
                f = self.fail[v]
                while f and ch not in self.next[f]:
                    f = self.fail[f]
                self.fail[nxt] = self.next[f].get(ch, 0)
                q.append(nxt)

    def search(self, text: str) -> list:
        node = 0
        hits = []
        for i, ch in enumerate(text):
            while node and ch not in self.next[node]:
                node = self.fail[node]
            node = self.next[node].get(ch, 0)
            if node:
                hits.append(i)
        return hits
```

---

## 6.7 組み合わせ論（応用）

**霊夢**: 包除原理、聞いたことある！

**魔理沙**: 「少なくとも 1 個」より **「ちょうど k 個」** が欲しいとき、包含関係で足したり引いたりする。

**霊夢**: 集合の和を「被り付き」で数えるやつだよね。

**魔理沙**: 条件 A₁…A_m について、**少なくとも 1 個を満たす** 個数は、空集合から始めて、1 個選ぶと足し、2 個選ぶと引き…と交互にする。競プロでは **「ちょうど k 個」** や **coprime の個数** に直すことが多い。

### 包除原理のイメージ / 手順

1. 条件を集合（または bit）で表す  
2. 各部分集合 S について **|∩_{i∈S} A_i|** を数える（単調なら式で高速化も可）  
3. |S| が奇数なら引く、偶数なら足す（「ちょうど」版は別の係数）  
4. 答えを mod で整える

### いつ使うか

| キーワード | 候補 |
|------------|------|
| 少なくとも / ちょうど k 個の条件 | 包除原理 |
| 互いに素、coprime | 包除（素因数で割る） |
| 回転・反転で同一 | Burnside |
| 木の全域木の個数 | 行列ツリー定理 |

### 計算量

| m 条件 | 部分集合走査 |
|--------|----------------|
| m ≤ 20 前後 | O(2^m × 各カウント) |

### つまずき

- **「少なくとも」と「ちょうど」** で符号が逆  
- 倍数のカウントで **lcm を取り忘れ**  
- 2^m が重いのに単調性で高速化できるのを見逃す

### 包除原理

→ [solutions/python/06/inclusion_exclusion/](../solutions/python/06/inclusion_exclusion/) / [solutions/go/06/inclusion_exclusion/](../solutions/go/06/inclusion_exclusion/)

```python
def count_exact_k(properties, n, k):
    # properties[i] は i 番目の条件を満たす index の集合、など
    # 2^m 部分集合を走査して |∩| を数える典型
    from itertools import combinations
    ans = 0
    m = len(properties)
    for mask in range(1 << m):
        bits = bin(mask).count("1")
        # 共通部分のサイズ cnt を求める（問題依存）
        cnt = ...  
        if bits % 2:
            ans -= cnt
        else:
            ans += cnt
    return ans
```

```go
func countCoprime(n int) int {
	primes := []int{2, 3}
	ans := n
	m := len(primes)
	for mask := 1; mask < 1<<m; mask++ {
		bits, prod := 0, 1
		for i := 0; i < m; i++ {
			if mask>>i&1 == 1 {
				bits++
				prod *= primes[i]
			}
		}
		if bits%2 == 1 {
			ans -= n / prod
		} else {
			ans += n / prod
		}
	}
	return ans
}
```

### Burnside の補題（入門）

**魔理沙**: **回転・反転で一致するものを同一** とみなす数え上げ。置換 g の **固定点個数** を数え、群のサイズで割って平均する。

**霊夢**: 「回転しても同じ塗り方」の数を数えるの？

**魔理沙**: 例: ネックレスの k 色塗り。各回転 θ について「θ で不変な塗り方の数」を足し、回転の個数 n で割る。反転もあるなら群を拡張する。

```python
# 例: n 個の玉を k 色で塗る（回転同一）→ 問題に応じて固定点を数える
```

### 行列ツリー定理（入門）

**魔理沙**: 無向木の全域木の数は、**ラプラシアン行列** L（対角に次数、辺に -1）の **任意の 1 行 1 列を消した余因子** の行列式。黄色では「存在を知っている」で十分なことが多い。

**霊夢**: 行列式って重くない？

**魔理沙**: n ≤ 数百なら高斯消去で O(n³)。「木の全域木の個数」が答えそのもの、またはサブ問題になる ARC D 系で出る。

---

## 6.8 黄色まとめハンズオン

**霊夢**: AGC B って、聞くだけで怖い……

**魔理沙**: 黄色は **テンプレの組み合わせ** だ。次の表を進め、使った技法を 1 行でメモしろ。

**霊夢**: 1 問で複数テンプレが出ることもある？

**魔理沙**: ある。例: 木 + パス集計 → LCA、区間 → Mo、数列 → NTT。読んだら **オフラインか・mod か・木か** を 30 秒でタグ付けしてから [solutions/python/06/](../solutions/python/06/) を開く習慣をつけろ。

| 順 | 問題 ID | 想定スキル | 目安 |
|----|---------|------------|------|
| 1 | [arc139_c](https://atcoder.jp/contests/arc139/tasks/arc139_c) | DP + 二分 | ARC C |
| 2 | [arc141_c](https://atcoder.jp/contests/arc141/tasks/arc141_c) | グラフ・構築 | ARC C |
| 3 | [arc142_d](https://atcoder.jp/contests/arc142/tasks/arc142_d) | 木・LCA | ARC D |
| 4 | [arc144_c](https://atcoder.jp/contests/arc144/tasks/arc144_c) | 組合せ | ARC C |
| 5 | [arc146_d](https://atcoder.jp/contests/arc146/tasks/arc146_d) | フロー・マッチング | ARC D |
| 6 | [arc151_d](https://atcoder.jp/contests/arc151/tasks/arc151_d) | データ構造 | ARC D |
| 7 | [agc017_b](https://atcoder.jp/contests/agc017/tasks/agc017_b) | 貪欲・文字列 | AGC B |
| 8 | [agc021_a](https://atcoder.jp/contests/agc021/tasks/agc021_a) | ゲーム・xor | AGC A |
| 9 | [agc022_a](https://atcoder.jp/contests/agc022/tasks/agc022_a) | シミュレーション | AGC A |
| 10 | [agc023_a](https://atcoder.jp/contests/agc023/tasks/agc023_a) | 累積・尺取り | AGC A |

**進め方**

1. 問題を読み、**オフラインかオンラインか** を決める  
2. キーワードからテンプレ候補を 1〜2 個挙げる（表の「想定スキル」と照合）  
3. [solutions/python/06/](../solutions/python/06/) でテンプレを当てはめ、**AC または部分点** まで持っていく  
4. 解説を読んだら **自分のコードに 1 関数だけ移植**（丸写しより定着が速い）  
5. ARC D は **部分点** でも提出して学ぶ  

| テンプレ | 復習の合図 |
|----------|------------|
| 行列べき乗 | n が巨大で線形漸化 |
| LCA / 重心 | 木 + パス・距離 |
| Mo | 大量の区間クエリ・オフライン |
| NTT | 畳み込み・係数列 |
| Dinic | マッチング・容量 |
| SA / AC | 部分文字列・複数パターン |
| 包除 | ちょうど k 個・coprime |

索引: [12.md](12.md)

---

## Part 6 到達チェック

- [ ] 行列のべき乗で線形漸化が書ける  
- [ ] LCA（ダブリング）が書ける  
- [ ] Mo's algorithm の並べ替えと伸縮が説明できる  
- [ ] NTT で畳み込みができる（テンプレ利用可）  
- [ ] Dinic または最小費用流のテンプレを持っている  
- [ ] サフィックス配列の用途を説明できる  
- [ ] 包除原理の式を立てられる  
- [ ] まとめで ARC D を **1 問以上** 手を動かし、AGC A〜B を **合計 3 問以上 AC**  

**霊夢**: テンプレの引き出し、もう一個サイズ大きい箱になった感じ。

**魔理沙**: [07.md](07.md) は **Meet in the middle・永続データ構造・重軽分解** だ。黄色の木とフローがそのまま効く。進むぜ。

---

## まとめ

| 節 | 内容 |
|----|------|
| 6.1 | 行列べき乗、CHT |
| 6.2 | 重心分解、LCA |
| 6.3 | 平方分割、Mo |
| 6.4 | NTT / 畳み込み |
| 6.5 | Dinic、最小費用流、マッチング還元 |
| 6.6 | サフィックス配列、Aho-Corasick |
| 6.7 | 包除原理、Burnside、行列ツリー定理 |
| 6.8 | ARC C〜D + AGC A〜B |

← [05.md](05.md) | [目次](00-toc.md) | 次 → [07.md](07.md)（橙色）
