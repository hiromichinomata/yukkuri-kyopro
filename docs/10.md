# 付録 B — Go リファレンス

競プロでよく使う **Go 1.22+** の書き方早見表。`bufio` による高速入出力を前提とする。

← [09.md](09.md) | [目次](00-toc.md) | 次 → [11.md](11.md)（スニペット）

---

## 入出力

### テンプレート

```go
package main

import (
	"bufio"
	"fmt"
	"os"
)

func main() {
	in := bufio.NewReader(os.Stdin)
	out := bufio.NewWriter(os.Stdout)
	defer out.Flush()

	var n int
	fmt.Fscan(in, &n)
	// ...
	fmt.Fprintln(out, ans)
}
```

### 1 行・複数整数

```go
var a, b int
fmt.Fscan(in, &a, &b)

var n int
fmt.Fscan(in, &n)
a := make([]int, n)
for i := 0; i < n; i++ {
	fmt.Fscan(in, &a[i])
}
```

### 文字列 1 行

```go
var s string
fmt.Fscan(in, &s)
// 空白なし 1 文字列は Fscan で 1 トークン
```

### Scanner（行単位・スペース区切り）

```go
sc := bufio.NewScanner(os.Stdin)
sc.Split(bufio.ScanWords)
sc.Scan()
s := sc.Text()
```

---

## スライス

```go
a := make([]int, n)           // 長さ n、ゼロ初期化
b := make([]int, 0, n)          // cap n の空スライス
a = append(a, x)
copy(dst, src)
sort.Ints(a)
sort.Slice(a, func(i, j int) bool { return a[i] < a[j] })
```

2 次元:

```go
grid := make([][]int, h)
for i := range grid {
	grid[i] = make([]int, w)
}
```

---

## map

```go
cnt := map[int]int{}
cnt[x]++
if _, ok := cnt[x]; ok { ... }
```

キーが存在しない読み取りはゼロ値になる（`cnt[x]` は 0）。

---

## `container/heap`

最小ヒープ（`Less` で小さい方が先）:

```go
import "container/heap"

type Item struct {
	v, idx int
}
type PQ []Item

func (p PQ) Len() int            { return len(p) }
func (p PQ) Less(i, j int) bool  { return p[i].v < p[j].v }
func (p PQ) Swap(i, j int)        { p[i], p[j] = p[j], p[i] }
func (p *PQ) Push(x any)          { *p = append(*p, x.(Item)) }
func (p *PQ) Pop() any {
	old := *p
	n := len(old)
	x := old[n-1]
	*p = old[:n-1]
	return x
}

pq := &PQ{}
heap.Init(pq)
heap.Push(pq, Item{v: 0})
```

最大ヒープは `Less` を逆にするか、値を負にする。

---

## 数値・mod

```go
const mod = 1_000_000_007

func powMod(a, e int) int {
	res := 1
	a %= mod
	for e > 0 {
		if e&1 == 1 {
			res = res * a % mod
		}
		a = a * a % mod
		e >>= 1
	}
	return res
}
```

`math/big` は重いので、通常は 64bit + mod で足りる。

---

## 再帰

Go は深い再帰に弱い。**DFS はスタック実装** を推奨する。

```go
stack := []int{start}
for len(stack) > 0 {
	v := stack[len(stack)-1]
	stack = stack[:len(stack)-1]
	// ...
}
```

---

## よくあるミス

| 現象 | 原因 | 対策 |
|------|------|------|
| TLE | `fmt.Scan` のみ | `bufio` + `Fscan` |
| WA | `Print` と `Println` の違い | 問題の出力形式に合わせる |
| RE | スライス範囲外 | `0 <= i < len` |
| CE | unused import / variable | 提出前に `go build` |

---

## 本書との対応

| トピック | 参照 Part |
|----------|-----------|
| 基本・入出力 | [00.md](00.md), [01.md](01.md) |
| グラフ | [03.md](03.md) |
| データ構造 | [04.md](04.md) |
| 上級 | [06.md](06.md)〜[08.md](08.md) |

スニペット集: [11.md](11.md) | Python 版: [09.md](09.md)

← [09.md](09.md) | [目次](00-toc.md) | 次 → [11.md](11.md)
